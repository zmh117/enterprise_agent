# 个人外部 API 凭据只能由用户本人建立和轮换

普通用户必须在自己的认证管理会话中输入 ONES 密码，登录成功后平台只加密保存 Token 并立即丢弃密码。自助接口从认证会话取得当前用户 ID，不接受客户端指定其他目标用户。管理员可以查看凭据元数据、禁用或解绑，但不能查看 Token、代输密码或替用户轮换；界面按 ADR-0031 复用现有外部身份面板并区分本人和管理员模式。现有只保存 ONES 身份映射的用户保留原绑定并显示凭据待验证，由本人重新验证后启用 API Capability。
