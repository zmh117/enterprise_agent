# 测试预览完整展示业务字段但从结构中排除凭据

Capability Test 允许管理员填写模拟 Agent 输入，并使用自己的外部身份执行 Draft。测试页完整显示 Method、相对路径、Query、映射后的请求体和通过 Output Schema 的规范化输出，不对关键词、工作项编号、名称、外部 User ID 或 Team ID 等普通业务字段做掩码。密码、Token、Cookie 和认证 Header 必须在构建预览前从预览数据结构中排除，不允许先生成敏感值再依赖显示层脱敏。外部 API 原始响应仍只存在于当前调用内存，不显示、不保存；测试证据只记录既定的状态与摘要元数据。
