# Mapping Plan 只支持确定性投影和基础类型转换

第一版 Mapping Plan 支持字段重命名、对象层级调整、从 Agent 输入/系统上下文/固定常量写入请求、从外部响应受限路径读取字段、数组逐项投影，以及显式的字符串、整数、数字和布尔值基础类型转换。可选字段缺失时可以使用管理员配置的固定默认值。不支持条件判断、过滤表达式、字符串拼接、日期计算、正则替换、函数、脚本或部分成功策略。类型转换失败、必填字段缺失、数组越界或输出 Schema 不匹配时，整个调用按契约错误失败，不得返回部分规范化结果。平台在发布时将配置编译为不可变计划并完成静态校验。
