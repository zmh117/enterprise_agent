# Enterprise Agent 当前上下文

本文件只描述当前领域语言和系统边界。历史 API Platform 术语与决策位于 `docs/archive/legacy-api-platform/`，不得覆盖当前代码、主 OpenSpec 或活动变更。

## 核心领域语言

**内部用户（Internal User）**

平台中承载角色、业务应用权限和数据范围的人员主体。钉钉或 ONES 用户都是外部身份，不直接成为授权主体。

**外部身份绑定（External Identity Binding）**

内部用户与受信外部系统账号的已验证关联。它只证明主体对应关系，不保存可重复使用的登录凭据，也不自动授予业务应用或工具权限。

**ONES 身份验证证明（ONES Identity Verification Proof）**

当前用户在一次请求中临时提交的邮箱、密码和登录响应 Token。请求结束前必须丢弃；数据库只保留 ONES User ID、显示名称、已验证 Team、默认 Team 和验证时间。

**Agent Definition / Revision / Publication**

Agent 的稳定身份、追加式草稿和不可变发布。Definition 创建后固定 `python-v1` 或 `typescript-v1`；Publication 冻结模型连接、指令、Skill、执行策略、Runtime kind 和 MCP Tool Envelope。

**Business Application Publication**

业务应用的不可变运行装配，冻结 Agent Publication、可选 Workflow、Trigger、Delivery、会话/执行策略和 Agent MCP Tool Envelope 的显式子集。

**MCP Tool Manifest**

由代码发布的固定只读工具目录，包含稳定 tool identifier、模型描述、输入 Schema、只读语义和资源类型。管理端不能创建动态 Handler、任意 URL、SQL 模板、脚本、Shell 或通用 MCP Server。

**Tool Resource**

数据库、Redis 或 Loki 等受治理资源身份。连接内容通过 Draft、验证和不可变 Revision 发布；Secret 只以 `secret_ref` 引用，运行时不读取草稿。

**业务数据范围（Business Data Scope）**

角色在业务应用内获得的明确 environment、可选 base 和 workshop 集合。用户消息可以不完整或变化；Agent 根据消息和 Skill 选择 Tool Call 参数，服务端在每次调用时实时复核授权范围和唯一资源。

**Agent Job**

一次异步执行请求。Job 冻结用户主体、应用/Agent Publication、Runtime kind、MCP Tool Schema 与授权摘要，但不把某次自然语言推断出的 environment/base/workshop 固化为不可变路由目标。

**Agent Session**

平台为稳定聊天边界保存的连续上下文；它可以承载多个任务和 Agent Job，但不是任务文件的隔离边界。

**Schema Baseline Generation**

当前 schema 从 `100_baseline_v1.sql` 开始。空库执行 `100[,101...]`；精确 legacy 042 数据库通过不可变 manifest 验证后只登记等价 marker，形成 `001..042,100[,101...]`。

## 已确认的待提案领域语言

以下术语和关系来自文件工作区探索，已经获得业务确认，但在形成并接受 OpenSpec change 前仍是待提案设计，不表示当前实现或 canonical Requirement 已经具备相应能力。

**任务工作区（Task Workspace）**

Agent Session 内一个具有独立生命周期的任务文件上下文；同一 Session 可以依次拥有多个任务工作区，但同一时刻最多一个处于 `ACTIVE`。

_避免称为：Agent Session、业务应用工作区、Runtime 临时目录。_

**Job 沙盒（Job Sandbox）**

只服务于一个 Agent Job 的隔离运行时目录；它承载从精确文件版本物化出的本地副本和本次执行产物，但不是持久文件事实来源。

_避免称为：任务工作区、共享卷、持久文件目录。_

**Job 文件清单（Job File Manifest）**

Agent Job 创建时冻结的受授权工作区文件元数据和精确版本集合；它允许 Agent 选择相关文件，但不包含文件内容、凭据或对象存储位置。

**文件服务（File Service）**

任务工作区、文件、文件版本及其对象内容的唯一事实入口；它同时向 Agent 暴露 File MCP 接口，并只在服务端基础设施层解析 MinIO 凭据和操作对象。

**File MCP 接口**

文件服务面向 Agent 暴露的受治理工具接口；它不是独立文件服务、独立容器或第二个文件事实入口。

**文件提交意图（File Commit Intent）**

Agent 对一个明确 Job 沙盒文件发出的持久化声明；它指定创建新文件或基于精确基础版本提交新版本，未声明的沙盒变化不进入文件服务。

**提交暂存（Commit Staging）**

文件服务为一个文件提交意图临时接收和校验文件内容的中间状态；暂存成功不等于文件版本已经创建。

**群会话共享（Group Conversation Sharing）**

群聊任务工作区中的文件由当前群会话共同使用，群成员不配置逐人文件 ACL，但每次操作仍须以实际发送人通过平台身份、业务应用访问和同群会话校验。

**群共享副本（Group-shared Copy）**

经来源用户明确确认后，从其个人外部系统权限导入群聊任务工作区的独立文件副本；它保留来源血缘，但不与外部原件自动同步或互相写回。

**消息附件（Message Attachment）**

随 Channel 消息进入平台并保持消息来源关系的原始文件；任务工作区只引用它，其 canonical 默认保留期为 360 天且不由工作区生命周期决定。

**临时工作文件（Temporary Working File）**

只服务于一个任务工作区的工作副本、中间文件或未保存草稿；工作区过期时应清理其内容。

**保留文件（Retained File）**

由用户明确保存或已经交付的精确文件版本；它脱离任务工作区生命周期并按独立文件保留策略管理，第一阶段内部内容默认保留 360 天。

**文件（File）**

在任务和交付过程中保持稳定身份的逻辑文件；它指向且只指向一个当前文件版本。

**文件版本（File Version）**

因导入、生成或编辑形成的不可变文件内容快照；它记录父版本和来源血缘，创建后不得原地修改。

**冲突候选（Conflict Candidate）**

基于已不再是当前版本的基础版本生成、因并发冲突而未成为当前版本的临时结果；它只供检查和后续显式处理，不代表正式文件版本。

**钉盘文件引用（DingTalk Drive File Reference）**

指向同一钉盘在线文件的稳定外部资源身份；它不包含访问凭据，平台每次使用时仍须按当前授权主体重新通过钉钉权限校验。

**按需同步（On-demand Synchronization）**

平台在打开任务工作区或继续处理文件时检查钉盘在线文件的最新内容，并在内容变化后导入新文件版本的同步方式；它不承诺后台轮询或实时变更通知。

**钉盘新文件交付（DingTalk New-file Delivery）**

将 Agent 产出的精确文件版本交付为一个新的钉盘在线文件；它取得独立钉盘文件引用，不覆盖作为输入来源的钉盘在线文件。

**文件内容保留策略（File Content Retention Policy）**

由平台或租户级合规管理决定的保留文件内部内容期限；它不属于单个 Business Application Publication，第一阶段默认值为 360 天。

**任务工作区保留策略（Task Workspace Retention Policy）**

由 Business Application Publication 冻结、通过管理前端配置的任务工作区自然周期策略，可选择 Asia/Shanghai 时区下的 `DAY`、`WEEK` 或 `MONTH`，canonical 默认值为 `WEEK`。

## 已确认的待提案交付范围

- 文件工作区第一阶段只接受、读取、生成、版本化和交付 `.txt` 文件，用于验证完整工作区链路。
- 文件工作区第一阶段的 `.txt` 单文件大小上限为 15 MiB；该上限只约束新的文件工作区链路。
- 每个任务工作区第一阶段暂定最多包含 20 个逻辑 **文件**；同一文件的多个文件版本不重复占用逻辑文件名额。
- 每个任务工作区第一阶段最多保存 100 MiB 临时内容，计算尚未成为保留文件的工作文件版本和冲突候选的实际内容大小。
- 已成为 **保留文件**的内容和只引用而未复制的 **消息附件**不占用任务工作区临时内容额度。
- 新版本会使临时内容超过 100 MiB 时，平台必须在提交该版本前完整拒绝，不得留下部分对象、部分版本或错误的当前版本指针。
- 第一阶段 `.txt` 输入必须是 UTF-8，可接受 UTF-8 BOM；平台不得自动猜测或转换 GBK、UTF-16 等其他编码。
- Agent 新生成的 `.txt` 文件版本必须使用 UTF-8 且不写入 BOM。
- 第一阶段不部署或调用 `docling-serve`，也不把现有专用附件解析器扩展为通用文档解析层。
- `DOCX`、`XLSX`、`PPTX`、`PDF`、图片 OCR 和 Markdown 统一留到下一阶段，由内部 `docling-serve` 提供解析计算能力。
- `docling-serve` 是文件处理链路后的内部计算服务，不作为 Agent 可直接调用的 MCP Server。
- 上述第一阶段范围只约束新的文件工作区链路，不得静默删除或关闭当前已经存在的消息附件兼容能力。
- **文件服务**通过 **File MCP 接口**提供受控的工作区文件列举、精确版本物化、版本提交、版本保留和版本交付能力；它不负责理解、检索或编辑文件内容。
- 本次消息新上传或明确引用的文件必须自动物化到 **Job 沙盒**；任务工作区内其他文件默认只进入 **Job 文件清单**，由 Agent 根据用户请求按需选择物化。
- Claude Code Agent 只在当前 **Job 沙盒**内使用受限的 `Read`、`Grep`、`Write` 和 `Edit` 检索或编辑本地副本，不得把文件系统工具扩展到沙盒外路径。
- Agent 必须逐个文件显式发出 **文件提交意图**；Job 结束不得自动扫描或提交沙盒中的全部新增和修改内容。
- 用户明确要求修改既有文件或生成新文件时，该请求已经授权 Agent 在当前任务工作区提交对应的新文件版本，无需在处理完成后再次询问相同提交确认。
- 用户只要求分析、预览或建议时，Agent 不得据此提交对既有文件的修改；用户意图不足以区分分析与修改时必须先询问。
- 钉钉用户明确要求修改或生成文件时，成功提交的精确文件版本默认创建回当前会话的交付意图，无需再次询问；用户明确要求只保存到工作区时不得自动交付。
- 私聊文件只交付回当前私聊，群聊文件只交付回当前群；冲突候选、提交失败内容和未声明提交的沙盒文件不得自动交付。
- 文件提交采用两阶段流程：**File MCP 接口**只创建提交意图，Runtime 再把对应沙盒文件流式上传到文件服务，文件服务校验并完成版本提交。
- 模型不得把完整文件内容、Base64 文件或 MinIO 上传凭据放入 MCP 参数、模型上下文或审计；文件字节只能由 Runtime 与文件服务之间的受控流式接口传输。
- 提交既有文件的新版本必须携带文件 ID 和基础版本 ID；保留和交付必须指向精确文件版本。
- 任务工作区的创建、切换、关闭和过期由平台工作流管理，不作为 Agent 可直接调用的 MCP 工具。
- **File MCP 接口**第一阶段不提供删除、分享、目录管理、任意路径访问或由模型指定任意对象存储键的能力。
- **File MCP 接口**只接受平台签发并绑定内部用户、租户、Job 和精确工具 scope 的 Principal JWT。
- Principal JWT 只证明调用主体、Job 和授权范围，不得包含 MinIO Access Key、Secret Key 或 Session Token。
- **文件服务**必须在服务端通过平台管理的 Secret Reference 解析 MinIO 凭据；Agent、模型、MCP 参数、MCP 响应、Job 和审计不得接收或保存原始凭据。
- 模型不得在 **File MCP 接口**参数中声明用户、租户、工作区、回复渠道、Bucket 或对象键；服务端必须根据 Job 解析当前任务工作区、固定回复路由和受控对象位置。
- 每次 **File MCP 接口**调用都必须复核目标文件属于 Job 绑定的任务工作区，并拒绝任意文件 ID、其他会话或其他租户资源。

## 已确认的待提案领域关系

- 一个 **Agent Session** 可以包含零个或多个 **任务工作区**。
- **文件服务**是任务工作区、文件、文件版本、配额、生命周期、审计和 MinIO 对象操作的唯一事实入口。
- Agent 只通过 **File MCP 接口**调用文件服务；Web、渠道接入和文件后台任务通过受控内部 API 调用同一文件服务领域能力。
- 第一阶段不单独部署 `file-mcp` 容器；**File MCP 接口**与文件服务内部 API 共享同一文件领域规则和 MinIO 基础设施适配器。
- 第一阶段以 `file-worker` 替换现有 `attachment-worker` 容器，不让两个 Worker 长期并存；原附件队列和在途消息必须保持兼容并由 `file-worker` 继续消费。
- `file-worker` 负责钉钉消息附件下载、向文件服务导入内容、工作区过期处理、保留内容清理和提交暂存清理；它不得持有 MinIO 凭据或绕过文件服务直接操作对象。
- 现有 Delivery Worker 继续负责渠道交付，不并入 `file-worker`；后续 `docling-serve` 解析编排也不属于第一阶段。
- 因此第一阶段只新增 `file-service` 一个净新容器，并用 `file-worker` 替换 `attachment-worker`；不新增独立 `file-mcp` 容器。
- 每个 Agent Job 必须在 Runtime 容器内创建一个独立 **Job 沙盒**；不同 Job 不得共享本地目录，Job 结束后必须清理该目录。
- 文件服务必须在 Job 创建时根据当前用户、租户、任务工作区和授权范围冻结 **Job 文件清单**，其中每个文件必须指向当时的精确文件版本。
- Agent 只能从当前 **Job 文件清单**选择文件，不得请求任意文件 ID、之后新增的文件、路径、Bucket 或对象键。
- Agent 判断文件是否与当前任务相关，文件服务负责最终授权和精确版本解析；Agent 的选择本身不构成访问授权。
- 平台只能把 **Job 文件清单**冻结且调用时仍获授权的精确文件版本从 **文件服务**物化到 **Job 沙盒**，并同时保留本地路径、文件 ID 和基础版本 ID 的受控映射。
- 同一 Job 执行期间，文件服务不得因任务工作区当前版本变化而静默替换清单中的精确版本；新的文件或文件版本只进入后续新 Job 的文件清单。
- **Job 文件清单**冻结文件身份和版本，不冻结访问授权；用户、应用或会话访问在调用时已经失效的文件必须拒绝物化。
- Claude Code Agent 对本地副本的检索和编辑不会直接修改 MinIO 或形成文件版本；只有通过 **文件服务**完成提交后，内容才成为新的不可变文件版本。
- 每个 **文件提交意图**只能选择一个当前 Job 沙盒内的受控文件；既有文件必须绑定物化时记录的文件 ID 和基础版本 ID，新文件必须声明受限的文件名和类型。
- 文件服务为提交意图签发不透明的提交 ID；提交 ID 只标识一次 **提交暂存**，不能单独作为上传授权，Runtime 仍必须证明当前 Job 和提交意图绑定关系。
- 同一个提交 ID 必须是严格幂等边界；相同提交元数据和相同内容哈希的重试只能得到同一个文件版本 ID，不得重复创建文件或文件版本。
- 已成功提交但 Runtime 未收到响应时，Runtime 必须能够使用原提交 ID 查询并恢复同一个成功结果，不得创建新的提交意图代替结果确认。
- 同一个提交 ID 被用于不同文件、不同基础版本、不同元数据或不同内容哈希时，文件服务必须拒绝并记录安全冲突，不得覆盖首次绑定事实。
- Runtime 必须直接把选定沙盒文件流式上传给文件服务，不得取得 MinIO 凭据、直接写入 MinIO，也不得让模型读取或转发上传授权材料。
- 文件服务必须在接收流时实施大小上限、内容哈希和类型校验，并在最终提交前完成第一阶段 `.txt` UTF-8 编码校验。
- **文件服务**提交新版本时必须重新校验 Job、任务工作区、文件归属、基础版本、大小和配额，并由服务端生成对象位置后写入 MinIO。
- **提交暂存**只有在对象内容完整、所有校验通过且文件版本元数据成功提交后，才能原子转为可引用的文件版本；失败或超时的暂存对象不得出现在文件列表或当前版本指针中。
- 文件后台任务必须可重试地清理失败或超时的暂存对象，并保留不含文件内容和凭据的结果审计。
- 一个提交失败不得导致其他沙盒文件被隐式提交，也不得把部分对象解释为成功文件版本。
- 同一 Job 的每个文件提交意图必须独立记录成功、版本冲突或其他拒绝结果；部分文件提交失败不得回滚已经成功提交的其他文件版本。
- 只要 Runtime 正常完成、持久化最终回复并向用户准确说明每个文件结果，Agent Job 就保持 `SUCCEEDED`；系统不得为文件结果部分成功新增 `PARTIAL` Job 终态。
- 只有 Runtime 无法完成执行或产生最终回复、发生整体超时或满足现有 Job 失败条件时，Agent Job 才进入失败类终态；文件版本冲突本身是单个提交结果，不是整个 Job 的技术失败。
- Agent Job 成功、失败、取消或超时时都必须清理 **Job 沙盒**；未发出提交意图或未成功提交的本地修改不得被解释为已保存文件或当前文件版本。
- Runtime 异常退出未完成正常清理时，启动恢复或定期清理任务必须删除已无运行中 Job 归属的残留沙盒，且不得把残留目录交给后续 Job 复用。
- 一个 **Agent Session** 同一时刻最多有一个 `ACTIVE` **任务工作区**。
- 一个 **任务工作区** 可以承载同一任务连续产生的一个或多个 **Agent Job**。
- 已关闭或过期的 **任务工作区** 不自动进入后续 Job 上下文；跨工作区使用文件必须显式引用或导入。
- 没有 `ACTIVE` **任务工作区**时，首个文件输入或文件产出请求可以创建新工作区；普通文字问答不创建工作区。
- 已有 `ACTIVE` **任务工作区**时，连续追问和新增文件默认进入当前工作区；用户明确开始新任务或结束当前任务时才直接切换。
- Agent 判断请求可能属于另一任务时必须先取得用户确认，不得静默切换 **任务工作区**。
- 已关闭或过期的 **任务工作区**不得自动恢复；后续文件任务默认创建新工作区。
- **任务工作区保留策略**由创建工作区时使用的 Business Application Publication 冻结；前端修改只影响新 Publication 创建的新工作区，不追溯修改既有工作区。
- 旧 Business Application Publication 缺少任务工作区保留字段时稳定解释为 `WEEK`；新 Publication 必须显式冻结 `DAY`、`WEEK` 或 `MONTH`，管理前端默认选择 `WEEK`。
- `DAY` 在创建工作区的自然日结束、次日 `00:00` 到期；`WEEK` 使用周一至周日并在下周一 `00:00` 到期；`MONTH` 在创建所在自然月结束、下月一日 `00:00` 到期。
- 工作区创建时必须按冻结策略计算精确到期时间；后续用户活动不得把到期时间滚动到下一个自然周期。
- 任务工作区仍关联非终态 Agent Job 或该工作区文件的非终态交付时不得执行内容清理；该暂缓不得改变冻结的自然周期到期时间。
- Agent Job 和文件交付均进入终态后，若冻结的自然周期已经结束，任务工作区必须立即应用过期结果。
- 私聊 **任务工作区**只属于当前内部用户，不与其他私聊或群会话共享。
- 群聊 **任务工作区**使用 **群会话共享**；文件服务不保存或同步钉钉群成员清单，也不维护群内逐成员 ACL。
- 群成员只有在当前消息形成同一企业、Connector 和群会话的可信 Job，且实际发送人已绑定内部身份并拥有业务应用访问权时，才可编辑该群工作区文件。
- **群会话共享**不得允许其他群、私聊、任意文件 ID 或过期 Job 绕过会话隔离。
- 用户直接在群聊上传的文件和 Agent 为群任务生成的文件默认采用 **群会话共享**。
- 通过个人 ONES 或其他个人凭据取得的文件，只有在来源用户明确确认群内成员均可编辑后，才能创建 **群共享副本**并进入群聊任务工作区。
- **群共享副本**创建后由群成员共同编辑，平台只保留来源与导入人血缘，不自动读取外部原件更新，也不把群内修改写回外部原件。
- 来源用户未确认时，个人来源文件不得进入群聊任务工作区，也不得向其他群成员暴露内容。
- **消息附件**继续遵守消息附件保留策略，不因引用它的任务工作区过期而提前删除。
- 部署可以显式覆盖 **消息附件**保留期，但未配置时必须使用 360 天作为平台默认值。
- **消息附件**保留期从其原始创建时间起算；历史附件不得因文件工作区功能上线而重新获得完整保留期。
- 历史附件缺少到期事实时，应按原始创建时间补齐；已经到期的内容必须通过可重试、可审计的清理流程删除，不得在 schema 迁移事务中直接删除对象。
- 任务工作区过期时，平台清理其 **临时工作文件**及可读取的派生内容，但不删除独立保留的消息附件或保留文件。
- 用户明确保存文件或系统成功交付精确文件版本时，该版本成为 **保留文件**，不得随任务工作区过期。
- **保留文件**的内部二进制和派生内容第一阶段默认保留 360 天；该期限独立于任务工作区自然周期和消息附件保留期限。
- **保留文件**必须使用平台或租户级 **文件内容保留策略**，不得由 Business Application Publication 单独放宽、缩短或覆盖。
- **任务工作区保留策略**只决定任务工作上下文的自然周期，不得被解释为保留文件的合规存储期限。
- 每个文件版本第一次成为 **保留文件**时必须冻结当时有效的文件内容保留期限并计算准确到期时间；后续策略修改默认不得改写该事实。
- 平台或租户修改 **文件内容保留策略**时只影响修改后首次成为保留文件的文件版本，不追溯既有保留文件。
- 合规要求必须调整既有保留文件时，应通过独立、可预览、需授权且可审计的迁移流程执行，不得借普通配置修改静默批量缩短或延长期限。
- 每个精确文件版本的 360 天期限从它第一次因用户明确保存或成功交付而成为 **保留文件**时起算。
- 重复查看、下载、保存或重新交付同一个精确文件版本不得重置其保留期限；新文件版本在首次成为保留文件时建立自己的独立期限。
- 文件服务清理到期 **保留文件**时只清理平台保存的内部内容，不删除或缩短对应钉盘在线文件的外部生命周期。
- 文件内容删除后仍可保留文件 ID、版本 ID、安全来源摘要、Job、交付和删除审计，但不得继续返回二进制或提取文本。
- 一个 **文件**包含一个或多个不可变的 **文件版本**，并且同一时刻只允许一个文件版本作为当前版本。
- 每次编辑提交必须同时声明文件 ID 和作为编辑基础的文件版本 ID；只有该基础版本仍是当前版本时，平台才可创建新文件版本并原子切换当前版本。
- 基础版本已不是当前版本时必须返回版本冲突，不得覆盖当前版本；第一阶段 `.txt` 以及后续 DOCX、XLSX、PPTX 等格式都不得由文件服务自动合并。
- 版本冲突产生的结果只能作为 **冲突候选**随任务工作区临时保留，用户可以检查或下载它，但平台不得把它静默设为当前版本或保留文件。
- 用户继续处理冲突时，后续新 Job 必须同时物化最新当前版本和冲突候选，由 Claude Code Agent 根据用户指令生成合并结果。
- 合并结果仍必须以最新当前版本作为基础版本发出新的显式文件提交意图；文件服务不得因内容来自冲突处理而跳过正常版本、配额或授权校验。
- 只有取得稳定 **钉盘文件引用**的在线文件才能跟踪后续编辑；只有短期下载凭据而没有稳定资源身份的消息附件只能作为接收时的内容快照。
- 平台打开任务工作区或继续处理关联文件时，应使用当前授权主体执行 **按需同步**；钉钉拒绝访问时不得以历史凭据或其他成员身份绕过权限。
- **按需同步**应比较外部最新内容与内部当前文件版本；内容变化时必须导入新的不可变文件版本并保留外部来源血缘，不得原地改写既有版本。
- **按需同步**不要求平台持续轮询钉盘或依赖文件变更事件推送；没有实时通知不得被解释为外部文件没有变化。
- **按需同步**只适用于明确保留 **钉盘文件引用**的在线文件；作为独立内容导入的 **群共享副本**仍不与其个人外部原件自动同步。
- 内部文件内容按保留策略到期删除后，即使对应钉盘在线文件仍然存在且用户仍有权限，平台也不得通过既有钉盘文件引用自动重新读取或继续处理。
- 用户需要继续处理已清理内容时必须重新发送或上传文件；平台将其作为新的 **消息附件**和新的 **文件**处理，不恢复旧工作区、旧文件版本或旧解析内容。
- 第一阶段 Agent 产出的文件版本必须采用 **钉盘新文件交付**，不得自动覆盖或原地更新作为输入来源的钉盘在线文件。
- 钉钉消息中的明确修改或生成请求默认同时授权把成功提交的结果版本采用 **钉盘新文件交付**返回当前会话；“只保存到工作区”必须抑制该默认交付。
- **钉盘新文件交付**成功后必须记录新钉盘文件引用、对应的精确内部文件版本和输入来源血缘；交付失败不得虚构外部文件引用或成功状态。
- 原钉盘在线文件在 **钉盘新文件交付**后继续由钉钉管理，平台不得把新文件的权限、内容或生命周期反向解释为已修改原文件。
- **钉盘新文件交付**不得为同一任务文件另建内部文件身份；Agent 产物应作为原 **文件**的新 **文件版本**延续内部版本链。
- 原钉盘文件引用继续关联作为输入的早期文件版本，新交付取得的钉盘文件引用只关联本次交付的精确文件版本。
- 钉盘文件引用是文件版本的外部呈现关系，不替代稳定的内部文件身份；同一个内部 **文件**可以随版本演进关联不同钉盘在线文件。
- 文件版本提交和钉盘渠道交付是两个独立事实；新文件版本通过基础版本校验并提交成功后即成为内部当前版本，不以随后渠道交付成功为前提。
- 钉盘交付失败不得回滚、删除或降级已经提交的当前文件版本；系统只重试该精确文件版本的固定交付意图，不得重新执行 Agent 生成另一份内容。
- 尚未由用户明确保存且尚未成功交付的当前文件版本仍属于任务工作区内容，不因版本提交成功自动成为 **保留文件**。
- 工作区到期时仍处于交付重试中的精确文件版本必须暂缓清理，以保证固定交付意图仍可完成；系统不得借此延长整个工作区的到期时间。
- 暂缓期间交付成功时，该精确版本成为 **保留文件**；交付最终失败时，如果自然周期已经结束，平台立即清理其临时内容。
- 无论交付成功、最终失败或过期清理，平台都必须保留文件版本、交付尝试和删除结果之间的审计关系。

## 已澄清的术语歧义

- “工作区”曾同时指业务应用管理页面、Runtime 临时目录和任务文件上下文；任务文件上下文统一称为 **任务工作区（Task Workspace）**。
- Runtime 容器内的本地目录统一称为 **Job 沙盒（Job Sandbox）**，不得称为任务工作区或作为跨 Job 持久化文件来源。
- “Agent 选择下载文件”统一表示 Agent 从 **Job 文件清单**判断任务相关文件，再由文件服务复核授权并物化精确版本；不表示 Agent 可以浏览或下载任意平台文件。
- “Job 使用工作区当前文件”统一表示使用 Job 创建时由 **Job 文件清单**冻结的精确版本，不表示执行期间自动跟随其他 Job 提交的新版本。
- “钉钉群权限”不表示文件服务复制钉钉成员或角色 ACL；它表示可信群消息证明当前发送人此刻来自该群，平台仍负责内部身份、业务应用访问和同群会话隔离。
- “把个人来源文件放进群里”统一表示创建 **群共享副本**，不是共享个人凭据、授权其他人访问外部原件或建立双向同步。
- “工作区删除文件”只表示清理 **临时工作文件**，不是提前删除 **消息附件**或已经提升的 **保留文件**。
- “编辑文件”统一表示基于明确基础版本创建新的 **文件版本**，不是原地改写既有版本；并发结果未通过当前版本校验时只能成为 **冲突候选**。
- “自动合并 TXT 冲突”不属于文件服务能力；冲突内容必须作为 **冲突候选**进入后续 Job，由 Claude Code Agent 在读取最新版本后完成显式合并和重新提交。
- “系统感知钉钉在线编辑”统一表示在下一次使用文件时执行 **按需同步**，不是承诺编辑发生时立即收到钉钉事件。
- “内部文件到期后继续处理”不属于自动恢复能力；用户必须重新发送文件，并按新的消息附件和新文件开始处理。
- “Agent 写回钉钉文件”在第一阶段统一表示 **钉盘新文件交付**，不是覆盖用户正在编辑的原钉盘在线文件。
- “交付为新的钉盘文件”不表示创建新的内部 **文件**；它表示同一内部文件的新文件版本取得独立的外部钉盘文件引用。
- “钉钉交付失败”不表示文件版本提交失败；内部当前版本保持不变，渠道交付沿独立状态机重试。
- “应用配置文件保留期”不属于 Business Application Publication；Publication 配置任务工作区周期，保留文件内容期限由平台或租户级 **文件内容保留策略**决定。
- “File MCP 操作 MinIO”统一表示 Agent 通过 **文件服务**的 **File MCP 接口**执行文件操作，再由文件服务基础设施层操作 MinIO；不表示 Agent 获得 MinIO 凭据、直接访问对象存储或存在独立 `file-mcp` 文件入口。
- “新增 file-worker”统一表示用职责扩展后的 `file-worker` 替换现有 `attachment-worker`，同时兼容原附件队列；不表示两个附件处理 Worker 长期并行部署。
- “Claude Code 编辑工作区文件”统一表示 Claude Code 在 **Job 沙盒**内编辑物化副本，再通过 **文件服务**提交新文件版本；不表示 Claude Code 直接编辑 MinIO 对象或绕过版本规则。
- “Job 完成后保存结果”统一表示逐个处理明确的 **文件提交意图**，不是自动收集或提交 Job 沙盒中的所有新增、修改和临时文件。
- “用户要求修改或生成文件”已经构成提交对应不可变文件版本的授权，不需要完成后重复确认；“分析、预览或建议”不构成修改既有文件的授权。
- “钉钉中要求修改或生成文件”默认包含把成功结果交付回当前会话，不表示覆盖输入原件、跨会话发送或交付冲突候选。
- “MCP 提交文件”统一表示 MCP 创建提交意图后由 Runtime 通过受控流式接口上传，再由文件服务完成 **提交暂存**和版本提交；不表示模型把完整文件内容编码进 MCP 请求。
- “重试文件提交”统一表示使用原提交 ID 恢复或重复同一个幂等操作；不表示重新生成文件版本或以新提交意图掩盖未知结果。
- “部分文件提交失败”不表示 Agent Job 必须使用 `PARTIAL` 或 `FAILED`；Job 终态描述 Agent 执行是否完成，每个文件提交意图另行保留精确业务结果。

## 示例对话

> **用户：**“分析这份计划表。”
> **系统：**“已为该任务创建任务工作区。”
> **用户：**“再增加负责人列。”
> **系统：**“继续使用当前任务工作区。”
> **用户：**“现在帮我处理另一份合同。”
> **系统：**“这是加入当前任务工作区，还是开始新的合同任务？”

## 当前运行链

```text
DingTalk / Webhook / Debug
  -> API Control Plane + PostgreSQL + RabbitMQ
  -> Agent Worker
  -> Python Runtime or TypeScript Runtime
  -> tool-mcp
  -> Published Resource Revision + Secret Ref
  -> Job / Tool Call / Delivery / Audit
```

Worker 负责可靠调度和 Runtime 选择，不内嵌模型 SDK 或工具实现。两个 Runtime 分别安装对应 Claude Agent SDK 并执行模型循环。`tool-mcp` 是标准 MCP Server，负责工具 Schema、实时身份/RBAC/应用/数据范围复核、资源解析和有界只读适配器调用。

## 治理边界

- 保留：统一身份、RBAC、业务应用授权、Agent/Application Publication、Resource、Secret、审计、Job/Delivery 历史。
- 退役：API Capability、Handler、API Connection、Application Resource Mapping、Internal API Platform、个人 API 调用 Token、旧 Runtime Tool MCP 与 HS256 signing key。
- MCP 不新增独立 Token、JWT、RBAC 或复杂治理层；它替换工具传输协议，不替代平台现有治理。
- ONES 身份绑定独立于 MCP。未来 ONES MCP 调用凭据必须另行设计，不能复用一次性登录材料。
- 工具只读且固定，不提供任意网络、文件写入、Shell、脚本或通用执行器。

## 当前事实入口

- 项目和运行边界：[README.md](README.md)
- 文档事实层级：[docs/README.md](docs/README.md)
- OpenSpec 主规格：`openspec/specs/`
- 活动实现变更：`openspec/changes/`
- 活动 schema：[backend/migrations/README.md](backend/migrations/README.md)

讨论新设计时必须区分“当前实现”“活动规范目标”“日期化验收”和“历史归档”。
