## 1. 公共契约

- [x] 1.1 统一合法标签名、精确值及 Schema，移除多层名称白名单并提供明确中文错误码。

## 2. 范围强制执行

- [x] 2.1 四类 Loki 工具强制资源固定范围；禁止 Agent 提交固定 key，发现不回退全局请求。
- [x] 2.2 更新工具说明与种子 Schema 哈希，保持现有 Job 冻结契约和授权边界。

## 3. 验证和交付

- [x] 3.1 增加跨层、固定范围、非法输入、HTTP 请求范围和错误分类回归测试。
- [x] 3.2 完成相关测试、静态检查、Compose 和 OpenSpec 严格校验，记录部署及真实验收边界。

## 4. 单次行数限制补充

- [x] 4.1 平台默认上限统一为 10000，资源默认与新建/编辑上限统一为 1000，保留显式配置和已发布版本。
- [x] 4.2 补齐前后端边界、显式覆盖、资源较小上限、默认 limit 和多次调用不累计的回归测试。
- [x] 4.3 执行相关测试及静态/构建/规格校验，记录升级与真实验收边界。
