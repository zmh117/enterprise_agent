# Validation evidence

初始双 Runtime 实现曾通过 TypeScript lint/typecheck/unit/contract/build、Python 协议/Worker/probe/readiness 聚焦测试、Compose 配置、OpenSpec strict validation 和差异检查。

该历史结果不证明当前 checkout。当前重验由 `retire-legacy-api-platform-for-mcp` 的任务 9 记录，必须覆盖：

- Python/TypeScript Runtime 协议与 Tool 等价；
- 固定标准 `tool-mcp`；
- Runtime Grant 与 MCP 无关；
- Job/Tool Event/Runtime provenance/Delivery 真实证据；
- Secret、Token、完整 Prompt 和 private thinking 残留扫描；
- 无旧平台服务、配置或数据表。
