## 1. 基线核对与测试设计

- [x] 1.1 记录当前未提交文件并核对渠道入口、身份解析、用户/外部身份、管理导航和现有测试边界，实施期间不得清理或覆盖无关修改
- [x] 1.2 为“从未绑定、身份停用/解绑、所属用户停用、已绑定可用”四类运行时结果建立后端测试夹具，并先写出不得创建 Agent Job/Outbox 的失败测试
- [x] 1.3 为私聊、群聊、同人多机器人、重复事件、文本截断、附件降级和异常 `createAt` 建立不含真实凭据的安全事件夹具

## 2. 候选投影数据模型

- [x] 2.1 新增候选聚合表与候选消息表迁移，包含租户用户唯一约束、来源事件唯一约束、外键、版本号和稳定分页/保留期所需索引
- [x] 2.2 新增候选、候选消息和身份发现状态的领域模型及 Repository 接口，明确待绑定、需恢复和已可用隐藏语义
- [x] 2.3 实现 PostgreSQL Repository 的幂等 upsert、最近 20 条裁剪、观察次数、跨会话来源汇总和并发版本处理
- [x] 2.4 添加迁移前进/回退和 Repository 集成测试，证明不同租户隔离、同租户同用户聚合、重复来源事件不重复计数

## 3. 钉钉入口安全投影

- [x] 3.1 在身份解析结果中稳定区分从未绑定、身份停用/解绑、所属用户停用和其它拒绝原因，不通过错误文案推断分类
- [x] 3.2 实现消息安全摘要转换器：文本/Markdown 纯文本 1,000 字截断，附件仅白名单类型、限长名称和受限大小，不保留原始嵌套对象
- [x] 3.3 将候选投影接入已认证且已持久化的钉钉身份拒绝事务，保证投影提交后才确认拒绝，失败保持 fail-closed 和可重试安全错误
- [x] 3.4 处理重复 Inbox/渠道事件重投，在已有入口事件路径上幂等补齐候选投影，且始终不创建 Agent Session、User Message、Agent Job 或 RabbitMQ Job 消息
- [x] 3.5 补充入口集成测试，覆盖私聊、群聊、历史身份、多 Connector、重复投递、投影失败和非身份拒绝不建候选

## 4. 候选查询与保留策略

- [x] 4.1 实现候选查询应用服务，使用服务端接收时间过滤 30 天窗口并实时关联外部身份与用户状态
- [x] 4.2 实现按名称、用户 ID、群 ID、机器人和会话构成筛选，以及 `(last_seen_at, id)` 稳定游标分页和最近消息展开
- [x] 4.3 实现列表、详情和不同候选人数计数的专用 DTO 白名单，确保已绑定可用身份立即从所有结果排除
- [x] 4.4 在现有 worker/服务调度边界实现幂等批次清理，只删除过期候选投影及消息，不新增容器且不删除渠道事件或审计数据
- [x] 4.5 补充查询与清理集成测试，覆盖有效/异常钉钉时间、30 天边界、`99+` 所需计数、绑定后即时隐藏和原始记录不受清理影响

## 5. 管理 API 与可信绑定

- [x] 5.1 新增候选列表、计数和详情管理接口，统一要求 Session 与 `identity:manage`，并验证未认证/无权限请求不泄露候选存在性
- [x] 5.2 新增候选绑定接口，请求只接收目标用户及候选/用户期望版本，服务端锁定候选并重新解析租户、用户 ID 和最近有效 Connector
- [x] 5.3 复用现有外部身份服务完成绑定，拒绝服务账号、停用用户、过期版本、失效 Connector、伪造身份字段和已有历史归属转绑
- [x] 5.4 为历史身份返回原人员和具体恢复状态，只允许进入原人员详情显式恢复，不提供自动启用或其它目标人员绑定
- [x] 5.5 为候选绑定成功与失败写入不含消息正文和敏感材料的安全审计，并将所有管理端错误映射为中文可操作提示和稳定内部错误码
- [x] 5.6 补充 API、CSRF、RBAC、并发和审计测试，覆盖创建人员后绑定失败仍保留人员、绑定/恢复成功立即隐藏和绑定前消息不回放

## 6. 管理端导航与发现页面

- [x] 6.1 将侧边栏改为“用户与外部身份”分组，保留 `/users` 并显示“人员管理”，新增 `/users/dingtalk-discovery` 路由和“未绑定钉钉用户”菜单
- [x] 6.2 实现候选 API Client、领域类型和 TanStack Query hooks，列表前台 15 秒、徽标前台 30 秒轮询，后台暂停并在恢复时立即刷新
- [x] 6.3 实现统一候选列表、稳定分页、搜索、全部/私聊/群聊/两者筛选、手动刷新、最近消息展开和安全纯文本/附件摘要显示
- [x] 6.4 实现菜单徽标并将超过 99 的数量显示为 `99+`，确保列表与徽标复用一致的 30 天和身份可用性条件
- [x] 6.5 实现“去绑定”候选上下文，仅传递内部候选 ID，在人员管理中选择可用自然人或“新建人员并继续绑定”
- [x] 6.6 复用人员详情钉钉绑定面板并自动打开，以服务端候选详情只读展示企业、Connector、用户 ID 和昵称，不允许客户端编辑可信字段
- [x] 6.7 实现历史身份“前往原人员恢复”、绑定成功返回列表并刷新徽标、失败保留上下文重试，以及全量中文加载/空态/错误/成功文案
- [x] 6.8 明确移除或禁止回复、发送消息、忽略、人工删除、批量处置、附件预览/下载和可执行 Markdown/HTML 入口

## 7. 自动化测试与安全验证

- [x] 7.1 补充前端单元/组件测试，覆盖导航、筛选、轮询可见性、徽标、纯文本转义、只读身份字段、新建后绑定失败和历史恢复分支
- [ ] 7.2 补充浏览器端到端测试，覆盖发现列表到选择人员、新建人员、可信绑定、即时消失和中文错误恢复流程
- [x] 7.3 运行后端完整测试、Ruff、前端测试、Lint、类型检查和生产构建，并修复所有由本变更引入的回归
- [x] 7.4 执行数据库、API 响应、日志、审计和浏览器状态安全扫描，确认不存在原始 payload、`sessionWebhook`、下载材料、Secret、Token、模型 API Key 或未截断正文
- [x] 7.5 执行候选突发写入、重复事件和稳定分页验证，确认唯一约束、20 条裁剪、轮询查询和清理不会产生重复、越界或无界增长

## 8. 容器重建与真实运行链路验收

- [x] 8.1 严格校验本 OpenSpec change，并确认 proposal、design、两份 delta spec 和任务清单与实现一致
- [x] 8.2 重建并启动受影响的 API、Admin Web、DingTalk Runtime 和 worker 容器，确认数据库迁移完成且所有服务健康
- [ ] 8.3 使用新的真实钉钉私聊消息验证 Runtime → Inbox → Channel Event → 身份拒绝 → 候选投影 → 管理列表，确认没有 Job、模型调用或回复
- [ ] 8.4 使用新的真实钉钉群聊消息验证用户名、用户 ID、群 ID、所属机器人、消息和时间展示，以及同一人员跨会话聚合
- [ ] 8.5 在管理界面完成现有人员绑定与新建人员后绑定，验证成功立即消失、失败可重试、旧消息不回放、绑定后的新消息才进入 Job → Worker → Delivery
- [ ] 8.6 使用停用/已解绑历史身份执行真实恢复链路，验证只能返回原人员、恢复后立即消失且不能转绑其它人员
- [ ] 8.7 汇总容器状态、测试结果、真实数据库记录和安全日志证据，确认未输出任何 Client Secret、Token 或模型 API Key
