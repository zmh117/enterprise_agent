## 1. 实施前核对与验收分层

- [x] 1.1 核对当前 canonical、前置清洗/索引 change 同步状态和知识代码事实；如前置规范已同步则重验本 delta，记录差异，不改动无关分页 change 或用户未提交工作。
- [x] 1.2 核对当前 migration catalog、索引读取合同、JWT 精确 scope 和 Runtime 模型调用路径，建立脱敏 evidence 模板，分别列出代码、合成、容器、真实 ONES、内网模型和业务效果证据。

## 2. 本地评测基线工具

- [x] 2.1 实现评测集格式与严格校验，支持 query_id、明确 KB/来源、人工相关文档/工作项 ID、无答案、分类及标注来源；提供纯合成样例、受限目录约定和 Git 排除保护。
- [x] 2.2 实现复用现有检索内核的本地评测 runner，记录数据集/语料/索引/profile/hash、代码与参数版本，输出文档级 Recall@10、MRR@10、无答案误报、耗时与失败分类的脱敏汇总。
- [x] 2.3 增加评测单元测试，覆盖多相关项、未命中、重复块、空相关集、标签缺失、失效文档、权限后可见相关集合和日志脱敏；原文自查询与人工效果不得混报。

## 3. 来源绑定与知识检索资源存储

- [x] 3.1 设计并实现独立来源绑定修订、检索资源/版本及验证事实的前向迁移，沿用同一 PostgreSQL knowledge schema，补当前双引擎 catalog、字段注释、约束和迁移回归；默认未核验/未发布，不预置真实来源或授权。
- [x] 3.2 实现来源绑定核验/换版/撤销应用服务与安全审计，保存人工来源确认和技术交叉验证摘要；严格区分 instance/Team/project UUID 与显示名称，不存原始业务内容或凭据。
- [x] 3.3 验证新增绑定不改原 source/KB 状态、document/revision/chunk/point 身份和 hash，补导入与索引零重复/零重编码的回归，禁止修改 Qdrant 旧 payload。

## 4. 检索资源管理与运行解析

- [x] 4.1 实现知识资源 Draft/验证/发布/停用/归档及并发 revision 管理，绑定 KB、来源修订、READY index/profile/hash，一个知识库最多一个启用检索资源。
- [x] 4.2 实现受管理权限保护的资源 API 和安全状态投影，拒绝 environment/placement 伪范围、任意连接地址/凭据及未知字段，配置修改后使验证失效。
- [x] 4.3 实现每次调用解析当前发布版本、单调用版本固定及返回前状态复核，覆盖停用、来源撤销、换版竞争、非 READY、索引不匹配和无 fallback。

## 5. 角色知识库范围与实时授权

- [x] 5.1 为现有角色业务访问事实增加 typed knowledge_base_id 允许范围关系和前向迁移，接入业务 revision、委派上限、预览、原子保存及 authorization hash，不新增旁路 ACL 或显式拒绝配置。
- [x] 5.2 实现同一角色业务记录内的应用/Tool/KB 共同授权，首版只允许有效业务应用 Job，直接 Agent 拒绝且不新增旁路 ACL；“当前全部”展开当前 ID，未来新增不自动授权，平台管理员不绕过业务范围。
- [x] 5.3 补授权矩阵与撤权测试，覆盖同角色 app1 有 KB/app2 无 KB、未配置不否决其他完整允许、直接 Agent/其他应用授权拒绝、跨角色拼接拒绝、仅 ONES 可读/仅 KB grant、停用用户/成员/角色、并发保存、旧 hash 和列表/检索一致性。

## 6. 固定 ONES 可读性桥

- [x] 6.1 实现仅启用知识服务时配置的固定服务身份及平台内部可读性入口，双验服务身份和 Knowledge Principal，从持久化事实重新解析候选成员、同一 Job/用户/来源和详情 Tool grant。
- [x] 6.2 在平台桥内部复用既有完整 scope ONES Principal 签发，固定调用只读投影入口，Token 仅留内存；拒绝任意 actor/server/scope/Team/operation/URL/UUID 覆盖，不提供通用代调用接口。
- [x] 6.3 在 ones-mcp 增加非模型可见的内部可读性投影，独立覆盖认证中间件/请求大小/限流，复用本人身份/详情 Operation/一次 401 刷新/解析，核对 UUID、实例/Team 上下文和 source_project_id，不返回正文。
- [x] 6.4 增加桥接与 Provider 合同测试，覆盖跨 audience、无详情 Tool、来源不符、HTTP 200 业务失败、403/404、401 重验、429/5xx/超时、错误结构、归属变更和不可跨调用缓存允许结果。

## 7. Knowledge MCP 与有界召回

- [x] 7.1 固定注册 knowledge-mcp、knowledge_list_bases 和 knowledge_search 的 Manifest/schema/effect，接入独立 audience、完整 scope、Tool 目录、Agent/Application 发布子集及 Runtime 固定 Server policy，保留旧 Job 拒绝新增工具。
- [x] 7.2 实现授权 KB 目录和 50 项 keyset 分页，cursor 绑定 Job/用户/应用/Snapshot/授权与资源摘要；补 0/1/50/51/多页、重复/遗漏、篡改、过长、跨用户和变更失效测试，不输出文档信息或数量。
- [ ] 7.3 通过生产 MCP/ASGI 入口接通授权 KB/发布索引的 dense 召回、当前版本/收录检查和文档去重；至多 200 候选点、50 个工作项、4 并发、60 秒总 deadline 并服从 Job 剩余预算，严格输出已授权引用和每文档最多 3 条证据。`services/knowledge_mcp_server/` 入口、独立鉴权、安全 MCP 审计、断开通知和有界线程已有合成验收；仍缺统一数据库等待/阻塞依赖取消预算的故障注入验收，故不补勾。
- [x] 7.4 实现有界补候选和 partial，明确区分预算耗尽、权限过滤及 Provider 故障；覆盖输入边界、重复点、全部过滤、top_k 不满、检查中撤权、无原始文本/被拒绝计数泄漏和安全审计。

## 8. 简化聊天模型边界（2026-09-19 用户修订，替代原任务 8）

原 8.1 的部署认可实现记录留在 evidence；原 8.2/8.3 强制内网运行协议与会话继承需求已取消，不作为已实现任务补勾。以下按新决策验收。

- [x] 8.1 移除强制内网聊天认可文件、API 状态、Publication/Worker/Runtime 门禁和 SDK 特殊覆盖；不增加协议版本或 Runtime 数据库权限，不改写旧 Publication。
- [x] 8.2 保持本地 Embedding 和既有模型配置/冻结映射/签名合同，保留 Agent/Application 详情 Tool 依赖与 Worker 模型/摘要前授权校验。
- [x] 8.3 补合成回归：外部聊天配置可发布与解析、缺详情 Tool 拒绝、撤权先于模型/摘要、Runtime/Worker 最小镜像隔离，明确不调用真实外部模型。
- [x] 8.4 同步 proposal/design/delta/手册及验收门槛，保留诊断脱敏、双重授权和真实链路未完成状态。

## 9. Web 配置与 Agent 使用说明

- [x] 9.1 在 Web 工具资源增加知识库表单/列表/详情，展示独立存储、来源、索引和发布状态，提供安全验证/发布/停用操作及稳定中文错误，不展示任意连接/凭据配置。
- [x] 9.2 在角色业务授权增加 KB 选择、明确 ID 预览、并发保存和撤权反馈；补 Web 类型检查/组件测试，验证无权限隐藏不代替服务端授权。
- [x] 9.3 完善工具描述和 Agent 使用约束：先发现明确 KB，再检索引用，再使用现有 ONES 详情 Tool；不猜 Team、不把零命中解释为全库无数据、不把业务正文当系统指令。

## 10. 可选部署与工程回归

- [x] 10.1 将 knowledge-mcp 与固定内部连接/健康检查/受管服务凭据接入可选 knowledge Compose，核对 PostgreSQL 最小读取/治理权限及内部网络边界；主部署未启用知识组件时不增加必需服务或 Secret。
- [x] 10.2 将新增 backend 测试登记 test_suite_tiers.toml，执行知识模块、RBAC/Principal、Manifest/Publication、ONES、模型/Session 与相关迁移回归，以及 Ruff、MyPy、Web 检查；失败不得归因为无关后直接忽略。
- [x] 10.3 执行主/可选 Compose 配置校验、严格 OpenSpec 校验、Markdown 链接和 diff 检查，更新部署/排障/回退说明，保证未验收事项仍明确未完成。
- [ ] 10.4 在本地隔离测试环境以合成语料验证完整调用、资源发布、双权限、撤权、容器重启及未启用知识环境的兼容性，记录为合成/容器证据，不冒充真实 ONES。

## 11. 真实环境与业务效果验收

11.1 原来源确认验收经用户明确取消（由任务 14 取代），不补勾为已完成；真实逐项 ONES 校验仍由 11.4 验收。
- [ ] 11.2 验收本地/内网 Embedding 与 Agent 既有聊天模型的真实知识新 Job 链路；聊天模型可为外部服务，不再要求内网部署认可或全文不外发证明。缺真实调用证据则仍未完成。
- [ ] 11.3 用户批准正式部署后，核对非终态工作及镜像/schema 兼容，执行正式 Migrator 和可选服务部署并验证 readiness；显式配置知识资源、角色与新的 Agent/Application Publication，不改旧 Job 权限。

  2026-09-19 本机已批准并完成迁移 139、相关主服务/Web 更新、Embedding/Qdrant 恢复及只读依赖检查；未部署在线 MCP、未保存/发布知识资源或修改角色/应用授权，因此本项不补勾，详见 evidence。
- [ ] 11.4 使用两位真实 ONES 可读范围不同的测试用户创建新 Job，验收 KB/ONES 双权限、引用回源、工作项权限撤销、KB 撤权、服务重启和既有模型版本/会话访问边界；记录是否真实调用 Provider 和每项结果。
- [ ] 11.5 由人工完成并复核首批约 50 条真实问法和相关文档标签，存入受限目录；运行离线 dense 与双权限端到端评测，输出脱敏 Recall@10、MRR@10、无答案误报、p50/p95 和失败分类。
- [ ] 11.6 根据实测基线由用户确认质量/时延准入目标并完成复测，保留未达标项；更新 evidence 与运行手册，只有取得相应真实证据才勾选验收，不自动提交、推送或归档。

## 12. Knowledge DDD 分层整理（本次范围）

- [x] 12.1 提取无 I/O 的领域规则、稳定身份、分块/向量合同与模型工具依赖，保留原 hash/profile/payload。
- [x] 12.2 用实际用例端口隔离 SQL/事务、文件和 HTTP；应用服务不读取其他服务的数据库属性，不引入通用 CRUD 或无调用者扩展。
- [x] 12.3 更新 API/CLI/bootstrap/ONES/Embedding 和精简镜像接线，移除平铺兼容文件；新增分层与镜像测试并登记分类。
- [x] 12.4 完成知识流水线/授权/模型/Runtime 扩展回归、类型/格式/规格检查，记录未启用外部集成和真实验收边界；不部署、不提交、不迁移或重建数据。

## 13. 简化来源配置（用户确认）

- [x] 13.1 删除 Web 来源确认/核验表单及命令；知识资源从所属数据集继承唯一有效来源，只读展示，不要求摘要或 RUNNING Job ID。
- [x] 13.2 提供导入侧显式来源确认入口，系统生成确认记录摘要，前向迁移区分 CONFIRMED 与历史 VERIFIED；未知/撤销/变化来源仍拒绝，保留每次检索的 KB＋ONES 双重鉴权，不自动确认真实批次。
- [x] 13.3 补 Web、导入确认、发布、撤销、来源冲突和迁移回归，同步规格与手册；不执行正式迁移或部署。

## 14. 取消配置阶段 ONES 来源确认（用户再次确认）

任务 13 保留历史实现记录；其中“来源确认是保存/发布前提”由本节取代，不伪造确认记录。运行时 KB＋本人 ONES 双重校验不变。

- [x] 14.1 新草稿仅引用已有 KB 与 READY 索引，从本地数据派生来源与内容摘要；前向迁移允许不关联历史来源绑定，保留旧记录和原始/向量数据，旧版本须重新保存验证发布。
- [x] 14.2 Web 移除来源确认门禁；运行时使用本人默认 ONES 身份逐项验证 UUID/项目与可读权限，不依赖配置阶段地址/Team 确认，不返回缓存正文。
- [x] 14.3 补无来源绑定的保存/验证/发布、技术依赖故障、数据变化、双权限/撤权与迁移回归，同步规格和手册；分开记录工程证据与真实运行验收。
