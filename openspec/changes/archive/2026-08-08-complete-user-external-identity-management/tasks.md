## 1. 现状基线与契约收敛

- [x] 1.1 盘点现有用户、外部身份、Session、CSRF、RBAC、审计和钉钉身份解析代码，记录本变更可直接复用与必须补齐的接口
- [x] 1.2 为用户列表、用户详情、用户写操作、钉钉绑定、ONES 绑定和身份状态变更确定统一的请求、响应与错误码契约
- [x] 1.3 为 `dingtalk`、`ones` 建立后端 Provider 白名单和值对象校验，拒绝其它 Provider 写入
- [x] 1.4 增加回归测试，证明现有管理登录、业务应用页面和钉钉消息链路在身份功能改造前后的公开契约保持兼容

## 2. 用户目录后端

- [x] 2.1 扩展用户 Repository 的搜索、稳定排序和分页查询，并支持按已绑定身份展示名称定位用户
- [x] 2.2 补齐用户列表和详情 DTO，只返回基本资料、账号类型、状态、版本和脱敏身份摘要
- [x] 2.3 补齐创建人类用户的应用服务，校验用户名唯一性并安全处理可选初始密码
- [x] 2.4 补齐用户基本资料编辑和启停应用服务，使用 `expected_revision` 实现乐观并发
- [x] 2.5 实现用户停用后的管理 Session 失效，并保证重新启用用户不会自动启用已停用身份
- [x] 2.6 在用户与身份应用服务中拒绝为服务账号绑定个人钉钉或 ONES 身份
- [x] 2.7 为用户查询、创建、修改、并发冲突、启停、Session 失效和服务账号约束增加单元与集成测试
- [x] 2.8 验证用户管理接口继续执行 Session、CSRF、`user:manage` 和安全审计，不存在旁路写入口

## 3. ONES 身份验证基础设施

- [x] 3.1 增加 ONES 身份实例配置模型和环境变量解析，覆盖实例编码、基础地址、Host 白名单、超时、响应大小和本地 HTTP 例外
- [x] 3.2 在 `.env.example` 和部署配置说明中加入非敏感 ONES 身份配置，不增加新的功能开关
- [x] 3.3 定义 `OnesIdentityVerifier` 端口与只包含 UUID、显示名称、团队 UUID 和验证时间的规范化返回模型
- [x] 3.4 实现固定调用 `/project/api/project/auth/login` 的 ONES 验证适配器，禁止请求级 URL、重定向和环境代理继承
- [x] 3.5 实现生产 HTTPS、Host 白名单、连接/读取超时、响应大小上限和严格响应 Schema 校验
- [x] 3.6 将 ONES 无效凭据、连接不可用和响应非法映射为稳定的安全错误码，不向客户端透传上游正文
- [x] 3.7 保证 ONES 响应 Token 在适配器边界立即丢弃，且密码、Token 和原始响应不能进入 Repository、日志或审计模型
- [x] 3.8 为成功、无效凭据、重定向、超时、超大响应、非法 Host、非 HTTPS 和非法 JSON 响应增加适配器测试

## 4. 外部身份领域与管理 API

- [x] 4.1 收敛 `user_external_identity` 写入模型，钉钉和 ONES 的 `tenant_code`、`external_subject_id`、`connector_id` 与白名单 metadata 只能由服务端生成
- [x] 4.2 实现 `GET /api/admin/external-identity-providers`，仅返回钉钉和 ONES 的非敏感可用性与展示信息
- [x] 4.3 加固钉钉绑定应用服务，校验受信连接器和租户关系，并保证同用户幂等、跨用户冲突
- [x] 4.4 实现 `POST /api/admin/users/{userId}/ones-identities`，先验证一次性凭据再原子创建或幂等更新身份
- [x] 4.5 在 ONES 身份绑定中仅保存实例编码、UUID、显示名称、去重团队 UUID、验证方式和验证时间
- [x] 4.6 补齐外部身份列表、启用、停用和软解绑返回模型，并统一执行 `expected_revision`
- [x] 4.7 保证跨用户身份冲突返回 409 且不自动覆盖、迁移或物理删除历史身份
- [x] 4.8 为钉钉绑定、ONES 绑定、Provider 拒绝、幂等、跨用户冲突、并发冲突、启停和软解绑增加应用服务与 API 测试
- [x] 4.9 验证所有身份写接口执行 Session、CSRF、`identity:manage` 权限，并记录不含邮箱、密码、Token 或原始响应的安全审计

## 5. 钉钉运行时身份回归

- [x] 5.1 检查并统一钉钉入口的身份解析条件，只有启用用户的启用钉钉身份可以进入授权和 Job 创建
- [x] 5.2 保持未映射、停用或已解绑身份走现有安全错误投递路径，不静默丢弃钉钉消息
- [x] 5.3 增加钉钉身份绑定后可解析、停用后拒绝、解绑后拒绝、用户停用后拒绝和重新启用后的集成测试
- [x] 5.4 验证 ONES 身份不参与 Channel ingress、管理登录或 Agent 工具选择，不触发任何 ONES 业务调用

## 6. 前端用户上下文

- [x] 6.1 按 DDD 边界创建 `contexts/users` 的 domain、application、infrastructure 和 presentation 目录与公共导出
- [x] 6.2 实现用户列表、详情、创建、编辑和启停所需的类型、查询键、API Gateway、Query 与 Mutation
- [x] 6.3 实现 `/users` 真实用户列表页面，包含加载、空状态、错误、搜索、分页、刷新、账号类型和状态展示
- [x] 6.4 实现创建用户表单，完成字段校验、重复用户名反馈、提交防重复和密码字段安全清理
- [x] 6.5 实现 `/users/:userId` 用户详情与资料编辑，处理加载失败、404 和乐观并发冲突刷新
- [x] 6.6 实现用户启用和停用确认流程，并在成功后准确失效用户目录和详情缓存
- [x] 6.7 启用“用户与外部身份”导航并接入两个真实路由，保持其它未交付管理入口禁用或隐藏
- [x] 6.8 为用户列表、创建、编辑、并发冲突和启停交互增加前端组件测试

## 7. 前端外部身份上下文

- [x] 7.1 按 DDD 边界创建 `contexts/external-identities` 的 domain、application、infrastructure 和 presentation 目录与公共导出
- [x] 7.2 实现 Provider、钉钉租户、身份列表、绑定、状态变更和解绑所需的 API Gateway、Query 与 Mutation
- [x] 7.3 在用户详情中实现钉钉和 ONES 身份卡片，展示 Provider、租户/实例、外部主体、展示名称、状态、验证时间和最近使用时间
- [x] 7.4 实现钉钉绑定对话框，只允许选择受信连接器/租户并填写 `senderStaffId`，处理幂等和冲突结果
- [x] 7.5 实现 ONES 绑定对话框，只允许填写邮箱与一次性密码，不提供 UUID、Token、URL、团队或 metadata 输入
- [x] 7.6 确保 ONES 密码仅保存在表单局部状态，提交成功、失败或对话框关闭后立即清空，且不进入 URL 或浏览器存储
- [x] 7.7 实现身份启用、停用和软解绑确认流程，处理 `expected_revision` 冲突并刷新用户详情
- [x] 7.8 为钉钉绑定、ONES 绑定、密码清理、冲突、身份状态变更和服务账号禁用绑定入口增加前端组件测试

## 8. ONES Mock 与端到端验证

- [x] 8.1 复核并补齐独立 `docker-compose.ones-mock.yml` 和 Mock 登录契约，提供成功、无效凭据和非法响应测试场景
- [x] 8.2 使用 ONES Mock 完成后端端到端测试，验证成功绑定、失败不落库、幂等绑定和跨用户冲突
- [x] 8.3 使用真实管理 Session 和 CSRF 在浏览器中验证用户列表、创建、详情编辑、启停、钉钉绑定、ONES 绑定、身份启停和解绑
- [x] 8.4 使用已绑定测试用户验证钉钉消息可创建现有 Agent 请求，并验证停用/解绑后的消息会收到安全错误而非静默无回复
- [x] 8.5 扫描数据库、审计事件、应用日志、API 响应、浏览器存储和前端构建产物，确认不存在 ONES 明文密码、Token 或原始登录响应
- [x] 8.6 验证页面中没有角色、授权、Session、Connection、冲突中心、ONES 需求/任务/缺陷、API Capability 或其它越界功能入口
- [x] 8.7 运行后端测试、前端测试、静态检查、生产构建和 Docker Compose 健康检查，并记录可复现的验证命令与结果
