## 1. 领域模型与迁移基线

- [x] 1.1 为钉钉企业四态生命周期、Corp ID 规范化、允许的状态转换和不可变约束编写领域模型单元测试
- [x] 1.2 为钉钉身份“企业 + Staff ID”唯一性及“用户 + 企业”单一当前身份编写仓储约束测试
- [x] 1.3 为应用观察、昵称事件游标和 ONES 使用事实定义后端 DTO、枚举与端口接口
- [x] 1.4 盘点 `user_external_identity`、`integration_connector`、候选、Ingress、Secret 和应用发布外键，记录 migration 与重建命令的精确删除顺序
- [x] 1.5 确认下一数据库 schema 版本并为升级、空库初始化和重复迁移建立测试夹具

## 2. 数据库结构

- [x] 2.1 新增 `dingtalk_enterprise` 表、Corp ID 唯一约束、生命周期检查、Revision 和审计时间字段
- [x] 2.2 为钉钉 Stream `integration_connector` 增加企业外键并移除新写入路径中的自由租户字符串语义
- [x] 2.3 重建或调整 `user_external_identity`，为钉钉身份增加企业外键和昵称观察游标，移除单一 Connector 所有权语义，同时保留 ONES 当前数据
- [x] 2.4 建立钉钉身份按企业和 Staff ID 唯一、每用户每企业单一当前身份的数据库约束与索引
- [x] 2.5 新增钉钉身份应用观察表及“身份 + 应用连接”唯一索引
- [x] 2.6 新增钉钉昵称变化审计表及按身份、时间查询索引
- [x] 2.7 将钉钉身份候选改为引用企业，并保持候选消息来源应用和幂等事件约束
- [x] 2.8 为 ONES 凭据增加最近尝试、最近成功、最近错误码和错误时间字段，并把 Team 候选升级为可保存名称与 ID 的结构
- [x] 2.9 编写迁移测试，证明旧 ONES User ID、默认 Team、Token 密文、状态和 Revision 非破坏保留，旧 Team ID 可转换为名称为空的候选
- [x] 2.10 编写迁移测试，证明常规 schema migration 不包含任何钉钉测试数据删除

## 3. 钉钉企业治理后端

- [x] 3.1 实现钉钉企业 Repository：列表、详情、创建草稿、乐观锁改名、停用、归档和恢复准备
- [x] 3.2 实现企业 Service 的状态机、Corp ID 不可编辑、归档前应用停用检查和恢复后强制复验
- [x] 3.3 复用 `channels.read/manage/restart/delete` 权限保护企业读取和治理操作，并为跨权限负向场景补充测试
- [x] 3.4 为企业名称变更、验证、停用、归档和恢复写入不含消息正文与认证材料的治理审计
- [x] 3.5 新增企业管理 API 与严格请求／响应 Schema，确保 Corp ID 不能由普通编辑请求提交
- [x] 3.6 为企业 API 添加乐观锁、Corp ID 冲突、非法状态转换和无权限测试

## 4. 钉钉应用连接归属与管理界面 API

- [x] 4.1 将 `DingTalkApplicationInput` 的自由 `tenant_code` 替换为受治理 `dingtalk_enterprise_id`
- [x] 4.2 更新 ManagedChannel Repository 和 Service，使每个钉钉应用连接必须引用一个企业并返回企业名称、状态和 Corp ID 验证摘要
- [x] 4.3 保持 Client ID／Client Secret 的 Secret reference 边界，证明企业 API 和连接响应不返回 Secret 或密文
- [x] 4.4 区分企业验证状态与 Runtime `CONNECTED/RECONNECTING/ERROR` 状态，不再用 SDK `registered=false` 生成“待注册”业务结论
- [x] 4.5 使待验证、停用或归档企业的连接不进入业务应用可用渠道候选
- [x] 4.6 更新连接创建、编辑、重连、删除和引用检查测试，覆盖同企业多应用与 Corp ID 不匹配状态

## 5. 企业验证与 Stream 入口

- [x] 5.1 扩展 Stream 解析与规范化模型，安全提取 `senderCorpId`、`chatbotCorpId`、`senderStaffId`、`senderNick`、事件时间和稳定事件 ID
- [x] 5.2 实现待验证企业首条受信测试消息的 Corp ID 校验和事务固化
- [x] 5.3 确保企业验证消息只写安全验证证据，不创建 Channel Outbox、身份候选、应用观察、Application Access 或 Agent Job
- [x] 5.4 对已验证企业的 Corp ID 缺失或不一致消息失败关闭，并写入不含消息正文和认证材料的治理告警
- [x] 5.5 在 Stream 重连、事件重试和在途消息处理中重复校验企业状态，防止连接状态绕过企业治理
- [x] 5.6 实现企业验证事件与普通业务事件各自的稳定幂等确认，保证验证事件重投不会转成业务 Job
- [x] 5.7 添加待验证、验证成功、字段不一致、企业停用、重连不匹配和重复事件的集成测试

## 6. 钉钉身份、候选、观察与昵称

- [x] 6.1 更新身份 Repository，按企业 ID 和 Staff ID 解析钉钉身份，不再读取身份行单一 Connector 归属
- [x] 6.2 移除管理员手工输入 Corp ID、Staff ID、昵称和来源连接的钉钉绑定 API 与前端契约
- [x] 6.3 更新发现候选 Service，使候选只由 `ACTIVE` 企业的受信消息产生并按企业 + Staff ID 聚合
- [x] 6.4 实现候选绑定的服务端可信字段重读、来源应用复核、目标用户状态和乐观锁校验
- [x] 6.5 实现同企业显式换绑事务：软解绑旧当前身份并创建或恢复新身份，保留其他企业身份
- [x] 6.6 保持历史身份只能恢复到原人员的规则，并覆盖跨人员转移拒绝测试
- [x] 6.7 实现应用观察记录首次创建、最近时间单调更新和事件重试幂等
- [x] 6.8 实现昵称事件时间校验、异常时钟回退、稳定事件 ID 决胜和非空单调更新
- [x] 6.9 在昵称实际变化时原子写入精简审计，并证明空昵称、旧事件和重试不会写重复审计
- [x] 6.10 调整钉钉业务消息事务顺序：企业／身份复核 → 最近使用／观察／昵称 → Application Access → Job
- [x] 6.11 添加同企业多应用、跨企业相同 Staff ID、每用户多企业、同企业换绑、身份停用覆盖全部应用的测试
- [x] 6.12 添加身份事实写入失败时不创建 Job，以及未绑定消息只形成候选的失败关闭测试

## 7. ONES 身份与凭据事实

- [x] 7.1 更新 Verification Challenge 确认事务，保存最新 Team 名称与 ID 集合、默认 Team 和 ONES 用户名称
- [x] 7.2 重新验证时整体替换 Team 候选，移除已撤销 Team 并保留“切换默认 Team 必须重新验证”的规则
- [x] 7.3 将 ONES 用户名称限定为登录响应 `user.name`，缺失时使用固定安全占位而不回退平台人员姓名或登录邮箱
- [x] 7.4 在 governed API 外部请求开始时更新个人凭据最近尝试时间
- [x] 7.5 仅在 Mapping Plan 和 Output Schema 完整成功后更新最近成功使用，并把审计来源区分为 `ADMIN_TEST` 与 `RUNTIME`
- [x] 7.6 在终态失败时更新最近错误码和错误时间，不覆盖最近成功时间；保持 401 失效和 403 不失效语义
- [x] 7.7 证明登录／重新验证与 Connection 启动验证不会更新个人凭据业务使用事实
- [x] 7.8 添加重试后成功、映射失败、Schema 失败、401、403、管理员测试和 Agent Runtime 的使用事实测试

## 8. 本人和治理身份 API

- [x] 8.1 定义分离的本人外部身份响应与管理员治理响应，使用白名单 Schema 而非序列化完整数据库记录
- [x] 8.2 本人钉钉响应只返回昵称、企业名称、状态、最近使用以及可展开的本人 Staff ID 和 Corp ID
- [x] 8.3 管理员钉钉响应增加绑定确认、身份 Revision 和按应用名称汇总的观察时间，不返回内部 Connector ID 或昵称历史正文
- [x] 8.4 本人 ONES 响应返回用户名称、综合可用状态、默认 Team、最近验证、最近成功使用、User ID 和 Team 明细
- [x] 8.5 管理员 ONES 响应分别返回身份与凭据状态，并增加允许的 Revision、Connection 发布版本、最近尝试和错误事实
- [x] 8.6 保持人员详情始终为治理模式、我的外部身份始终为本人模式，即使管理员查看自己的人员记录
- [x] 8.7 添加响应泄漏测试，证明 Token、密码、密文、认证 Header、Client Secret、Session Webhook 和 Challenge 内部数据不出现在任何身份 JSON
- [x] 8.8 添加本人越权请求应用观察、凭据 Revision、错误码和他人身份时失败关闭的 API 测试

## 9. 渠道与企业前端

- [x] 9.1 在“渠道与触发器”中增加钉钉企业列表、创建、改名、停用、归档和恢复确认界面
- [x] 9.2 在钉钉应用连接表单中移除自由租户输入，改为选择企业；首个应用支持先创建待验证企业
- [x] 9.3 分别展示企业生命周期和连接运行状态，为首个应用提供“已连接，等待企业验证”和发送测试消息指引
- [x] 9.4 对 Corp ID 冲突、不匹配、待验证和恢复复验显示安全中文提示，不展示消息正文或 Secret
- [x] 9.5 在企业停用、归档和连接删除确认中列出受影响应用连接及业务应用引用
- [x] 9.6 更新渠道管理前端 Schema、查询缓存、乐观锁处理、权限控制和可访问性测试

## 10. 外部身份前端

- [x] 10.1 将钉钉本人卡改为昵称、企业名称、状态和最近使用摘要，并提供只读 Staff ID／Corp ID 展开区域
- [x] 10.2 将钉钉治理卡改为友好摘要和技术详情，按应用名称显示观察记录且不展示 Connector ID
- [x] 10.3 把人员详情“绑定钉钉”改为选择受信候选，删除手工 Staff ID、租户和昵称表单
- [x] 10.4 为同企业换绑提供明确影响说明和单独确认，不把普通“绑定”点击当作换绑授权
- [x] 10.5 将 ONES 默认卡改为用户名称、综合可用状态、默认 Team、最近验证和最近成功使用
- [x] 10.6 将默认 Team 展示为“名称（ID）”，把其他 Team 收纳到“可用 Team”折叠区域
- [x] 10.7 实现本人 ONES 账户详情与管理员技术详情的不同字段集合，移除固定租户、连接器、Revision 和原始 `ACTIVE` 默认展示
- [x] 10.8 为缺失昵称、缺失 Team 名称、凭据缺失／失效／停用、最近错误和历史连接已清理编写中文空态与错误态
- [x] 10.9 添加键盘操作、屏幕阅读标签、移动布局和本人／治理权限边界的组件测试

## 11. 一次性钉钉测试数据重建命令

- [x] 11.1 实现重建计划收集器，精确列出旧钉钉连接、Secret、Runtime、租约、Ingress、Outbox、候选、身份、观察、审计和活动渠道绑定
- [x] 11.2 实现默认只读预检输出，包含环境、数据库指纹、分类计数、保留项和确定性 `plan_hash`
- [x] 11.3 实现生产环境硬拒绝、固定确认文字、显式执行参数、写入停止检查和计划 Hash 复核
- [x] 11.4 实现外键安全的单事务清理，软删除旧连接、撤销专属 Secret并使活动路由不可用
- [x] 11.5 保持历史 Application Publication 引用不变并把旧连接投影为不可运行历史来源
- [x] 11.6 添加保护断言，禁止删除任何人员、ONES、Capability、Agent、业务应用主体、Agent Job、Tool 调用或投递记录
- [x] 11.7 实现失败整体回滚、成功后数量复核和重复执行空计划幂等语义
- [x] 11.8 添加预检无写入、生产拒绝、计划漂移拒绝、事务回滚、保留历史和成功重建的 CLI 集成测试

## 12. 自动化验证与回归

- [x] 12.1 运行并修复后端 identity、identity discovery、managed channel、DingTalk Stream、governed API runtime 和 migration 测试
- [x] 12.2 运行并修复前端 users、external identities、managed channels、applications 组件与路由测试
- [x] 12.3 增加 API 合约测试，覆盖本人／管理员字段白名单和旧通用字段不再渲染
- [x] 12.4 增加端到端测试：创建企业 → 首个应用连接 → 测试消息验证 Corp ID → 候选绑定 → 新消息创建 Job
- [x] 12.5 增加同企业第二应用端到端测试，证明复用身份、创建第二观察记录且应用访问仍按当前连接计算
- [x] 12.6 增加企业停用、Corp ID 不匹配、同企业换绑、昵称乱序和应用观察写入失败的负向端到端测试
- [x] 12.7 增加 ONES 重新验证 Team 名称、管理员 Test 和 Agent Runtime 成功／失败使用事实的端到端测试
- [x] 12.8 运行前端 lint、typecheck、完整测试和 production build
- [x] 12.9 运行后端完整测试、迁移空库／升级测试和 OpenSpec strict 校验

## 13. 本机运行环境重建与真实验收

- [x] 13.1 在执行任何清理前备份当前数据库并记录恢复方法，停止 DingTalk Runtime、Ingress Dispatcher 和相关 Outbox Worker
- [x] 13.2 应用新 schema migration，验证 API 健康、数据库版本和现有 ONES 绑定／凭据仍完整
- [x] 13.3 运行钉钉测试数据重建只读预检，把数据库指纹、计划 Hash、分类数量、受影响应用和保留项提交给用户
- [x] 13.4 只有用户回复固定文字“确认清空钉钉测试数据”后，才使用同一计划 Hash 执行事务清理并保存复核结果
- [x] 13.5 重新构建并启动 API、worker、admin-web 和 DingTalk Runtime，确认所有服务健康且旧连接不再活动
- [x] 13.6 通过管理页面创建真实企业草稿和本机测试应用连接，发送测试消息完成 Corp ID 验证
- [x] 13.7 从新受信候选绑定测试人员，为业务应用选择新连接并重新发布
- [x] 13.8 使用真实钉钉私聊和群聊验证昵称、企业名称、应用观察、身份隔离、Job 创建和最终回复
- [x] 13.9 使用本人和管理员会话分别验收钉钉／ONES 卡片字段、展开边界、治理动作和认证材料不泄漏
- [x] 13.10 验证历史 Agent Job、Tool 调用和投递记录仍可读取，并把旧连接显示为不可用历史来源

## 14. 文档与交付

- [x] 14.1 更新 managed multi-DingTalk 运维文档，说明企业创建、应用加入、验证、停用、恢复和排障流程
- [x] 14.2 更新外部身份管理文档，说明本人／治理模式、候选绑定、同企业换绑、观察记录和 ONES 字段语义
- [x] 14.3 更新测试数据重建 Runbook，明确非生产限制、预检、固定确认、备份、回滚和保留数据
- [x] 14.4 对照 ADR 0049、Proposal、Design 和 6 份 Delta Specs 复核术语、状态和数据边界一致
- [x] 14.5 运行 `openspec validate model-dingtalk-enterprises-and-identity-observations --strict` 并修复全部错误
