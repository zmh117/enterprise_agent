## 1. 前置条件与基线

- [x] 1.1 确认 `add-role-and-authorization-control-center` 的实现、迁移和严格校验全部完成，记录当前 migration head
- [x] 1.2 执行 `git status --short` 并登记与本变更相关及无关的未提交文件，确认后续不清理、不覆盖现有修改
- [x] 1.3 为身份、角色、授权、Webhook、业务应用 Trigger、历史 Event/Job/Audit 和钉钉候选建立脱敏测试基线夹具
- [x] 1.4 记录变更前 API、Worker、Runtime、登录和角色授权测试结果，确保基线失败与本变更区分

## 2. 数据模型与迁移

- [x] 2.1 新增 `identity_authorization_reset_operation` 表、阶段状态约束、影响/备份安全摘要字段和单活动操作约束
- [x] 2.2 为 `user_password_credential` 增加 `must_change_password`、初始凭据到期时间及兼容迁移默认值
- [x] 2.3 为历史 Webhook Event 和其它强主体引用增加最小安全主体快照字段
- [x] 2.4 将只用于历史解释的主体外键迁移为可空 `ON DELETE SET NULL`，保留现有数据并补齐索引
- [x] 2.5 新增持久化身份授权维护状态、revision 和写入者租约所需数据结构
- [x] 2.6 为 PostgreSQL 与测试用 SQLite 实现等价迁移和约束，验证迁移重复执行安全性
- [x] 2.7 增加迁移测试，证明历史记录保留、当前配置引用可识别、旧密码凭据无需强制改密

## 3. 依赖目录与预检

- [x] 3.1 实现身份与授权删除范围目录，覆盖人员、服务账号、密码、外部身份、会话、角色、成员和全部新旧授权表
- [x] 3.2 实现 PostgreSQL 外键目录扫描，将每个主体引用分类为删除、快照置空、替代停用、清空重配、文本快照或阻断
- [x] 3.3 实现应用级逻辑引用登记，覆盖 `subject_code`、owner、Trigger Binding、Webhook Trigger、Publication 快照和审计操作者字段
- [x] 3.4 对未知强引用采用默认阻断策略，并返回不含主体明文和凭据的中文报告
- [x] 3.5 实现 `report` 演练命令，输出各类待删除、待保留、待替代、待停用和待重配数量及稳定摘要
- [x] 3.6 增加预检测试，证明新增未知外键会阻止准备，已登记引用不会被漏报

## 4. 备份、准备与维护锁

- [x] 4.1 实现仓库外新路径校验和受限权限目录校验，拒绝相对路径、仓库内路径及已有输出文件
- [x] 4.2 实现 PostgreSQL custom-format 备份，安全继承连接配置且不在命令参数或日志中输出数据库凭据
- [x] 4.3 实现备份 SHA-256、文件大小、数据库版本和 migration head 登记，并用 `pg_restore --list` 验证目录
- [x] 4.4 实现 `prepare` 命令，固定影响清单摘要、备份摘要、确认到期时间和操作 ID
- [x] 4.5 实现数据库持久化维护状态和 PostgreSQL advisory lock，保证同一时间只有一个重置执行者
- [x] 4.6 为 API、身份绑定、授权写入、DingTalk/Webhook Inbox、Job Dispatcher、Agent Worker 和 Delivery Worker 增加维护状态门禁
- [x] 4.7 实现写入者租约和有界排空，存在未停止写入者时不得开启删除事务
- [x] 4.8 增加备份失败、摘要变化、确认过期、锁冲突和写入者超时测试

## 5. 历史快照与服务账号替代

- [x] 5.1 实现旧主体最小安全快照生成器，只保存账号类型、不可逆主体摘要和有界显示标签
- [x] 5.2 在删除前为历史 Webhook Event 及其它强引用写入快照并解除当前主体外键
- [x] 5.3 按仍被运行配置引用的旧服务账号生成新 ID 和不可预测服务账号名，状态固定为 `disabled`
- [x] 5.4 改写 Webhook Trigger 和业务应用 `SERVICE_ACCOUNT` Trigger Binding 引用并原子停用对应入口
- [x] 5.5 清空业务应用 owner 等可选当前主体引用，生成中文重配清单
- [x] 5.6 保持不可变 Publication 和历史操作者文本不变，并在历史查询中标记“主体已删除”
- [x] 5.7 增加测试，证明业务配置和历史链路保留、替代账号无密码无角色无授权且入口不能创建 Job

## 6. 原子重置与管理员初始化

- [x] 6.1 实现交互式确认短语校验，确认内容绑定操作 ID 和影响摘要且不允许非交互式绕过
- [x] 6.2 实现按外键顺序删除旧角色授权、角色成员、角色、会话、密码、外部身份、直接策略、平台授权和旧账号
- [x] 6.3 在同一事务中创建唯一启用人员账号 `platform-admin`、受保护系统角色和启用成员关系
- [x] 6.4 确保系统角色隐式获得当前及未来全部 Web 管理能力，但不创建任何业务应用、工具或数据范围授权
- [x] 6.5 在同一事务中写入 `strict_application_role` 配置 revision，并移除重置后旧策略兼容求值入口
- [x] 6.6 实现提交前不变量校验：唯一人员管理员、零旧事实、零业务授权、零悬空引用、替代账号和依赖入口全部停用
- [x] 6.7 实现操作 ID 幂等与状态机保护，重复 `apply` 不得创建第二套主体、凭据或停用记录
- [x] 6.8 增加故障注入事务测试，逐阶段证明任一步骤失败都会恢复原身份、授权和入口状态

## 7. 一次性凭据与首次改密

- [x] 7.1 使用密码学安全随机源生成高强度初始密码并复用现有 Argon2 密码服务
- [x] 7.2 以 `O_CREAT | O_EXCL`、`0600` 和 `fsync` 写入仓库外凭据文件，标准输出只返回操作 ID、文件路径和安全摘要
- [x] 7.3 实现事务失败和异常恢复时的无效凭据文件识别与安全清理，确保失败操作对应密码不能登录
- [x] 7.4 在初始凭据上设置强制改密和到期状态，过期后拒绝登录并提供中文安全提示
- [x] 7.5 实现后端受限首次登录会话，只允许改密、退出和会话自检接口
- [x] 7.6 实现首次改密成功后更新 Argon2 哈希、清除强制状态、撤销全部会话并要求重新登录
- [x] 7.7 在管理端实现中文首次改密页面、路由守卫和错误展示，不在浏览器存储或遥测中记录密码
- [x] 7.8 增加凭据不落日志/命令/API、文件权限、过期、受限会话、改密和旧密码失效测试

## 8. 严格授权与审计

- [x] 8.1 让 API、Channel、Webhook、Worker 和 Delivery 的授权读取配置 revision，重置后不再接受旧用户或旧策略兼容来源
- [x] 8.2 让已删除钉钉身份重新进入未绑定候选流程，不创建 Agent Job
- [x] 8.3 让仅有 `platform-admin` 的新管理员运行应用时返回“当前用户无权使用该业务应用”
- [x] 8.4 让停用或未分配业务角色的替代服务账号在 Inbox/Job 创建前被拒绝，不回退到匿名或平台管理员
- [x] 8.5 实现重置操作安全审计，记录阶段、影响摘要、备份摘要、替代/停用数量、不变量和错误码
- [x] 8.6 检查所有新增日志、审计、异常和测试快照，确认不包含密码、Client Secret、Token、模型 API Key、消息正文或完整外部身份标识
- [x] 8.7 增加从入口到 Job 创建、Worker 执行前和 Delivery 前的严格授权回归测试

## 9. 恢复能力与运维文档

- [x] 9.1 实现 `verify` 命令，检查台账、凭据状态、唯一管理员、严格模式、旧事实、替代账号、入口停用和悬空引用
- [x] 9.2 实现 `restore-check`，校验操作 ID、备份摘要、数据库版本和迁移版本但不自动执行破坏性恢复
- [x] 9.3 编写中文运维手册，覆盖 report/prepare/apply/verify、维护窗口、凭据文件领取、首次改密、重配清单和异常处置
- [x] 9.4 编写提交后恢复手册，要求导出重置后安全审计、完整数据库恢复、迁移、容器重建和真实链路复验
- [x] 9.5 在隔离数据库完成一次完整备份恢复演练，记录恢复后核心表计数和授权模式验证结果

## 10. 自动化质量验证

- [x] 10.1 运行后端新增测试和身份、Webhook、业务应用、授权中心、DingTalk、Job、Delivery 相关回归测试
- [x] 10.2 运行后端全量测试与 Ruff，修复本变更引入的全部失败
- [x] 10.3 运行管理端单元测试、Lint、类型检查和生产构建，验证首次改密与维护提示均为中文
- [x] 10.4 运行 `dingtalk-runtime` 和其它受影响 TypeScript 服务的测试、Lint 与 typecheck
- [x] 10.5 运行 OpenSpec strict validate，确保 proposal、design、delta specs 和 tasks 全部通过

## 11. 隔离环境破坏性验收

- [x] 11.1 从真实结构的脱敏数据库副本执行 `report`，人工核对删除、保留、替代、停用和重配范围
- [x] 11.2 故意增加未登记主体外键，证明 `prepare` 被阻断；移除测试依赖后重新准备
- [x] 11.3 创建并验证备份，执行 `apply`；凭据文件只交付路径和安全摘要，不在终端或记录中展示密码
- [x] 11.4 执行 `verify` 并用 SQL 独立确认唯一人员管理员、旧身份/授权清空、业务配置与历史保留、严格模式生效
- [x] 11.5 使用一次性凭据完成首次登录和强制改密，确认初始密码与全部旧会话失效
- [x] 11.6 从登记备份完整恢复隔离数据库，重跑迁移并确认重置前身份、授权和业务链路可恢复

## 12. 容器重建与真实运行链路验证

- [x] 12.1 重建并启动 `api-server`、`admin-web`、`dingtalk-runtime`、`agent-worker` 和 `channel-dispatch-worker`，确认相关容器健康
- [x] 12.2 在维护模式下验证新登录、身份绑定、授权写入、DingTalk/Webhook Inbox、Job 领取和投递领取均被安全阻止
- [x] 12.3 退出维护后验证 `platform-admin` 可管理人员和角色，但无业务应用使用权
- [x] 12.4 发送新的钉钉私聊和群聊消息，验证旧外部身份出现在未绑定列表、消息/时间/用户名/用户 ID/群 ID/所属机器人可见且不创建 Job
- [x] 12.5 调用未重配 Webhook，验证停用替代服务账号在 Inbox 前或 Job 创建前被拒绝且无匿名回退
- [x] 12.6 管理员重新绑定一个测试用户并显式分配业务角色后，验证 DingTalk → Runtime → Inbox → Outbox → RabbitMQ → Job → Worker → Delivery 全链路成功
- [x] 12.7 检查容器日志、审计和数据库安全摘要，确认没有初始密码、Client Secret、Token、模型 API Key 或消息正文泄漏
- [x] 12.8 汇总测试、容器重建、恢复演练和真实链路证据，确认全部验收项完成后再交付重配清单
