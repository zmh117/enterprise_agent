# 验证记录（2026-09-17）

## 已实现

- Schema 服务响应中的 next_cursor 改为 19 字符短引用；原始游标保存在平台数据库，不进入新响应。
- 授权检查先于引用恢复；原 Codec 仍负责 Job、身份、授权快照、目标、query、资源修订和 checksum 校验。
- 每页仍最多 50 张表，不自动读完所有页、不放宽字段上限；保持旧长游标兼容。
- 新增迁移 135，位于工作区已有知识库迁移 134 之后；未重排或修改既有迁移。
- 临时映射 24 小时到期，tool-mcp 每分钟最多清理 500 条；仅 RUNNING Job 可使用，Job 删除级联删除。
- 存储故障返回 `mcp_pagination_store_unavailable` / FAILED；错误引用仍明确拒绝，不猜测、不退回旧权限或静默重查。

## 本地证据

- 16 个相关测试文件合计 **216 passed、1 skipped**。跳过项为需要专用真实知识库 PostgreSQL DSN 的既有验收，不是分页测试。
- 合成 Schema 目录 151 张表按 50/50/50/1 连续四页，位置准确且无重复遗漏；输出仅有短引用。
- 已覆盖旧长游标、错字、未知和跨 Job 引用、过期、终态 Job、引用碰撞、绑定变化、原始状态损坏、授权撤销、资源修订变化及存储故障。
- 使用一次性 PostgreSQL 18（仅合成数据）执行迁移 135 DDL；验证真实存取、跨连接恢复、到期与有界清理、Job 删除级联。
- 同一次性 PostgreSQL 中另建空库，全量迁移到 135、幂等重复执行及 SchemaHeadValidator 均通过。
- 既有 134 升级 135 的本地迁移回归通过；不重写 Job 数据。
- Ruff、相关源码 mypy、`git diff --check`、`docker compose config --quiet`、OpenSpec 严格校验通过。
- 一次性测试容器和合成数据库已删除，没有连接或改写现有业务数据库。

## 部署与验收边界

本次变更未构建/替换现有业务服务，也未访问报告问题的 Windows 主机。上述结果不是 Windows 真实 Job 验收。

部署时使用包含迁移 135 的一致版本，按既有 migrator 流程升级平台数据库，并同步更新受 schema readiness 约束的服务及 tool-mcp；不要混用只能识别长游标的旧 tool-mcp 实例。该迁移不需要连接业务数据库，不应复制或手动修改 Job 快照。前序迁移 134 已由独立知识库提交 `33d8a54` 纳入，发布时需包含完整迁移链。

新 Job 的验收：对同一目标、同一 query 连续查询至少四页，检查 next_cursor 为短引用、无 cursor invalid，核对 has_more 与完整表列表。切换 query 后的成功结果不能代替原目录分页完整性验收。若希望新工具中文说明进入冻结的发布配置，再按 Web 流程发布 Agent/应用；短游标本身未改变输入 schema。

真实环境任务 3.1 保持未完成，未归档。
