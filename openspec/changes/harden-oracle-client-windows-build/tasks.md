## 1. 构建兼容与失败判定

- [x] 1.1 增加 CRLF、不可执行脚本、空格路径、正常无匹配和检测异常回归，复现旧实现失败。
- [x] 1.2 固定 Oracle Python 构建脚本 LF，Docker 归一化行尾并显式使用解释器。
- [x] 1.3 区分 ZIP 无 19c 与检测故障退出码，保留客户端版本、位数和架构校验。

## 2. 验证与交付说明

- [x] 2.1 运行相关单元测试、脚本语法、配置与严格 OpenSpec 校验。
- [x] 2.2 在隔离 Linux 环境验证 CRLF 安装链路并记录结果，不连接真实 Oracle、不更换运行服务。
- [x] 2.3 更新 Windows 和目标主机镜像更新说明，记录本地证据与未执行的现场验收边界。

## 3. 已确认的依赖兼容补充

- [x] 3.1 增加 libaio t64 私有兼容链接，不覆盖既有库并覆盖失败场景测试。
- [x] 3.2 增加构建期真实 Thick 初始化及实际版本检查，覆盖初始化异常、Thin 和错误版本回归。
- [x] 3.3 使用实际 19.12 ZIP、CRLF 脚本完成隔离 AMD64 镜像验证，复测无客户端分支并更新交付说明。

用户已确认修复 `libaio.so.1` 缺失并补齐构建期 Thick 初始化检查；仍不替代真实 Oracle 服务端验收。
