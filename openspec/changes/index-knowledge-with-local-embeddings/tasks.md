## 1. 实现前预检与部署准备

- [x] 1.1 获得本提案确认，重新核对源码、migration head、已分块数量/摘要及运行服务；核实本轮范围不包含云端推理或 MCP 授权。
- [x] 1.2 核实 ARM64 支持，固定 BGE-M3 完整 revision、独立模型服务依赖和基础镜像/Qdrant digest；实现不接触业务数据的模型文件准备和清单校验。
- [x] 1.3 增加可选 Compose profile、internal 知识网络、只读模型卷、Qdrant 持久卷和资源限制；验证无默认公开端口，不给模型准备任务数据库访问权。
- [x] 1.4 按用户确认将知识部署拆到 `knowledge/compose.yml`，显式叠加主 Compose；保留项目/卷/网络身份和数据库边界，同步运行文档并验证默认部署不含知识组件。本项不执行迁移、重启或全量索引。

## 2. 本地 Embedding 服务

- [x] 2.1 实现模型加载/就绪及配置指纹、只读 token 计数、dense 批量向量化；离线本地文件加载，禁用远程代码、遥测及隐式截断。
- [x] 2.2 实现 token/请求体/批量/并发边界和安全错误处理；合成测试覆盖空输入、长输入、非法形状、忙碌、缺文件、模型指纹不一致及错误不回显文本。

## 3. 索引存储与客户端

- [x] 3.1 新增 knowledge.vector_index、vector_index_item 的前向双引擎迁移、外键/唯一约束/中文注释与事实源清单；不改原九表或历史迁移。
- [x] 3.2 实现固定内部端点的 Embedding/Qdrant 客户端；禁用代理/重定向，限制超时/重试，校验返回数量、顺序、维数、有限性及模型配置。
- [x] 3.3 实现分页固定语料清单、明确索引版本和单写者锁；校验当前来源版本、收录、chunk profile 和文本摘要，中断可补齐且不提前 READY。
- [x] 3.4 实现拥有明确身份的 collection 创建/校验、必要 payload 索引、稳定 point ID、确认写入与检查点；逐项恢复缺失点、拒绝身份冲突，不清空其他集合。
- [x] 3.5 增加只读预检、100 块基准、显式提交/恢复及全量核验 CLI；模型调用不放在数据库事务中，输出仅安全统计。

## 4. 检索入口与自动化验证

- [x] 4.1 实现受限查询 CLI：stdin 查询、READY/知识库限定、同模型编码、最多 200 候选、回源校验、文档去重、证据定位及不足结果 partial 标记。
- [x] 4.2 补合成故障测试：上写后未记账中断、检查点与点不一致、并发索引、数据版本变化、Qdrant 配置冲突、非法向量、HTTP 重试、旧版本/移除收录过滤及去重补足。
- [x] 4.3 在隔离 PostgreSQL/Qdrant 验证真实迁移、类型/约束、写确认、持久化、点恢复和幂等；运行独立模型服务合成 smoke 和网络/日志安全检查，登记 test suite tiers。
- [x] 4.4 按用户确认修复全量回归暴露的四处问题：迁移目录/向量集成版本断言、Oracle 私有票据的 HS256 扫描边界及服务身份常量导入；补防退化测试，重跑全量与隔离存储验收，不执行正式迁移或全量索引。

## 5. 正式本机执行与交付

- [x] 5.1 准备必要镜像和本机服务；验证离线网络，实际执行分层 100 块基准与全量 token 预检，记录耗时/吞吐/内存，无超限后再扩大执行。
- [x] 5.2 核对迁移窗口并通过 one-shot Migrator 更新 schema；执行已批准语料的全量索引，校验逐项身份及数量、原九表摘要不变，不静默处理数量漂移。
- [x] 5.3 完成全量重放不增量、故障恢复、服务重启持久性和检索 smoke；区分合成/原文自查询与尚未验证的人工业务召回效果。
- [x] 5.4 完成相关回归、Ruff/MyPy、Compose/OpenSpec strict、git diff --check 和中文运行/验收记录；明确未包含 Web/MCP/授权、混合召回、OCR 和在线采集，不把未完成项勾为完成。
