## 1. 基线与契约

- [x] 1.1 核对当前 `rbac_role`、`rbac_user_role`、`permission_policy`、`platform_access_grant`、业务应用、Agent job 和 delivery schema，记录必须兼容的字段与外键
- [x] 1.2 核对现有 `/api/admin/roles`、成员、权限、当前 principal capability summary 和平台 access grant API，确定保留、扩展和仅兼容的接口
- [x] 1.3 核对当前前端导航、人员详情、钉钉身份发现绑定和业务应用能力目录的数据契约，补齐 typed DTO 设计
- [x] 1.4 增加角色授权中心的授权模式配置契约，定义 `compatibility` 与 `strict_application_role`，默认使用兼容模式
- [x] 1.5 建立变更前数据库授权数据只读基线统计，确保后续 migration 不删除用户、身份、角色、成员、策略或 grant

## 2. 加法数据库迁移

- [x] 2.1 为 `rbac_role` 增加 system/custom 来源、保护标记、用途标签及 metadata/admin/business/membership 分区 revision
- [x] 2.2 为 `rbac_user_role` 增加 `expires_at`、分配人和安全来源字段，并补充到期查询索引
- [x] 2.3 创建角色管理能力绑定表及角色、能力、资源选择器唯一约束和查询索引
- [x] 2.4 创建角色业务应用访问表及角色、应用唯一约束和状态索引
- [x] 2.5 创建业务应用能力绑定表并增加应用访问、能力唯一约束和查询索引
- [x] 2.6 创建业务应用明确数据范围表，使用环境、基地、车间外键且不支持动态通配
- [x] 2.7 将现有 `platform-admin` 安全回填为受保护系统角色并初始化各分区 revision，不新增其业务访问权限
- [x] 2.8 编写 PostgreSQL migration 测试，证明旧数据和外键全部保留、重复执行安全且 migration 可在现有现场数据上完成

## 3. 领域模型与 repository

- [x] 3.1 实现统一角色聚合、受保护系统角色和不可变角色 code 的领域校验
- [x] 3.2 实现按 metadata/admin/business/membership 分区读取和 revision 并发控制的 repository
- [x] 3.3 实现管理能力绑定的批量替换、资源级唯一性和事务回滚
- [x] 3.4 实现业务应用访问、只读能力和明确数据范围的聚合读写 repository
- [x] 3.5 实现“当前全部”按操作者可授权集合展开为明确 scope ID 的服务，禁止未来资源通配
- [x] 3.6 实现成员批量添加、移除、启停和可选失效时间，确保过期成员不参与有效角色查询
- [x] 3.7 实现角色停用/启用且不物理删除，保留成员、授权和审计
- [x] 3.8 实现人员账号与服务账号成员类型校验，阻止服务账号加入含管理后台能力的角色

## 4. 管理能力目录

- [x] 4.1 将现有 `ADMIN_CAPABILITIES` 扩展为包含中文名称、模块、动作、风险、依赖、资源范围和可分配属性的唯一后端目录
- [x] 4.2 补齐工作台、业务应用、渠道、Agent 配置、人员、角色授权、运行记录、审计、平台配置和密钥等现有管理动作
- [x] 4.3 实现高级动作自动依赖查看能力的目录校验，保持编辑、发布、激活等职责独立
- [x] 4.4 实现能力目录只读 API 和 typed response，拒绝未知或不可分配能力 code
- [x] 4.5 将当前 principal capability summary 改为由统一目录计算，并返回前端导航所需的稳定能力集合
- [x] 4.6 实现 `platform-admin` 仅对管理能力目录的当前和未来能力自动允许，证明不会旁路业务应用或数据授权
- [x] 4.7 为菜单、按钮和所有现有管理 API 建立能力映射对账测试，发现未受后端强制校验的入口时补齐

## 5. 角色授权 application service

- [x] 5.1 实现角色创建、复制、模板、基本信息更新和建议 code，创建后禁止修改 code
- [x] 5.2 实现管理能力授权区候选集合校验、风险确认、变更原因和原子保存
- [x] 5.3 实现业务应用授权区候选集合校验、能力安全上限校验、明确 scope 展开和原子保存
- [x] 5.4 实现角色级 `roles.assign` 分配委派，分离角色编辑权和成员分配权
- [x] 5.5 实现操作者“可授权范围”求值，禁止授予未委派的应用、能力、数据范围或管理能力
- [x] 5.6 实现复制角色时不复制成员并阻止保留超出操作者可授权范围的项目
- [x] 5.7 实现最后一名启用 `platform-admin` 的事务锁和防锁死校验
- [x] 5.8 实现非最后一名管理员自撤权二次确认及成功后的当前会话权限刷新/撤销
- [x] 5.9 实现角色、授权区和成员变更的 secret-safe 审计，高风险变更记录原因，成员延期复用普通成员更新审计

## 6. 业务应用授权求值

- [x] 6.1 实现业务应用 invoke、应用能力和应用内明确数据范围的统一决策模型
- [x] 6.2 实现多角色允许并集、过期/停用排除和高级显式拒绝优先
- [x] 6.3 实现同一角色多个应用的数据范围隔离，禁止跨应用复用 scope
- [x] 6.4 实现业务应用 invoke 对固定 project 和 Agent 入口许可的封装，同时保留应用激活、publication 和 Execution Policy 校验
- [x] 6.5 实现 Debug/直接 Agent 调用与业务应用路由授权语义的清晰分流
- [x] 6.6 实现 `compatibility` 决策顺序、旧 project/agent 策略安全回退和 `legacy_compatible` trace
- [x] 6.7 实现 `strict_application_role` 模式，确保没有应用角色授权时 fail closed
- [x] 6.8 确保应用级显式 deny 在两种模式下都禁止旧策略回退
- [x] 6.9 实现人员与服务账号统一业务授权求值，服务账号不得因此获得 Web 管理能力

## 7. 有效权限解释与管理 API

- [x] 7.1 实现复用真实 evaluator 的 `AuthorizationExplanationService`，返回允许/拒绝、阶段、来源角色、应用、能力和范围安全摘要
- [x] 7.2 对解释结果中的策略条件、连接信息、消息正文、凭据、Token、Secret 和模型 API Key 做强制脱敏
- [x] 7.3 实现角色分页、搜索、系统/自定义/状态筛选及成员和授权摘要 API
- [x] 7.4 实现角色详情的 metadata/admin/business/membership 分区 typed API 和各自 expected revision
- [x] 7.5 实现角色成员批量 API 和人员详情多角色批量 API，统一调用同一 membership service
- [x] 7.6 实现业务应用、能力、local 环境、基地和车间的可授权目录 API，仅返回操作者可管理范围
- [x] 7.7 实现有效权限模拟 API，按用户、业务应用、能力和明确数据范围返回安全决策
- [x] 7.8 将现有用户直接策略和直接平台 grant 暴露为只读/受限编辑的高级授权例外 API，普通管理员不得访问
- [x] 7.9 为所有新写 API 接入 CSRF、session principal、授权、revision、field errors、correlation id 和中文安全错误

## 8. 钉钉绑定与人员授权

- [x] 8.1 扩展钉钉身份绑定 command 和 DTO，支持零个或多个初始角色及“仅绑定身份”显式确认
- [x] 8.2 在同一个数据库事务中锁定候选、用户和角色，完成身份绑定、成员创建、候选状态和关联审计
- [x] 8.3 任一角色不可分配、停用、越权或并发冲突时回滚完整绑定，保证候选继续留在未绑定列表
- [x] 8.4 绑定成功后返回合并的业务应用、数据范围和后台能力摘要
- [x] 8.5 用户仅绑定未授权时返回并展示“未获得应用权限”，不再暴露底层 project/agent 缺失提示
- [x] 8.6 增加私聊和群聊候选绑定到既有/新建人员并分配多个角色的后端回归测试

## 9. Agent 创建、Worker、工具和投递重校验

- [x] 9.1 确保新 Agent job 持久化稳定的业务应用 ID/code、请求主体和授权上下文，不只依赖易变 routing JSON
- [x] 9.2 在 Channel/Trigger 创建 job 前接入业务应用角色授权，并返回中文应用级拒绝提示
- [x] 9.3 在 Worker 将 PENDING 转为 RUNNING 前重新校验当前应用授权，撤权或过期时非重试失败且不调用模型
- [x] 9.4 在每次业务能力调用前重新校验角色、应用能力和明确数据范围，拒绝时不访问 Internal API Platform
- [x] 9.5 将工具集合收紧为注册只读、平台启用、应用装配、Agent publication、角色能力和应用数据范围交集
- [x] 9.6 拒绝通过任何角色 API 授予数据库写入、Redis 修改、Shell、文件写入或其它非只读工具
- [x] 9.7 在结果投递前重新校验应用权限，撤权时阻止业务结果并创建 `BLOCKED_BY_AUTHORIZATION` 或等价状态
- [x] 9.8 对支持的原 reply route 发送固定中文权限变更通知，通知失败时不回退其它目标且不重跑 Agent
- [x] 9.9 为创建、Worker、工具和投递四阶段生成统一 secret-safe 决策 trace 和关联审计

## 10. 前端角色授权中心

- [x] 10.1 在“用户与外部身份”下新增“角色与授权”菜单和 `/users/roles`、`/users/roles/:roleId` 路由
- [x] 10.2 将静态导航改为按当前 principal 管理能力显示，直接访问无权路由时展示中文拒绝
- [x] 10.3 实现角色列表的系统/自定义/高级例外区域、搜索、状态、成员数和授权摘要
- [x] 10.4 实现角色详情基本信息、成员、管理后台能力、业务应用与数据范围、有效权限预览和操作记录页签
- [x] 10.5 实现按目录分组的管理能力复选框、依赖联动、资源范围选择、高风险说明和模块只读批量选择
- [x] 10.6 实现业务应用 → 中文只读能力 → local → 基地 → 车间层级选择和“当前全部”明确集合提示
- [x] 10.7 实现授权区本地草稿、未保存离开提示、变更差异、受影响成员预览和分区原子提交
- [x] 10.8 实现无权授权区只读展示，确保提交 payload 不包含或覆盖不可编辑区域
- [x] 10.9 实现空白、模板和复制角色流程，复制时不包含成员并重新确认应用与范围
- [x] 10.10 在人员详情增加角色分配、有效期和有效权限来源展示
- [x] 10.11 改造未绑定钉钉用户流程，支持多个初始角色、仅绑定确认和绑定后权限摘要
- [x] 10.12 实现权限模拟表单和中文安全决策说明
- [x] 10.13 扫描新旧相关页面的用户可见英文提示和错误，统一为中文并保留安全内部编码

## 11. 后端自动化测试

- [x] 11.1 增加管理能力目录完整性、唯一 code、依赖闭包、未知 code 和平台管理员未来能力测试
- [x] 11.2 增加统一角色、多角色并集、deny 优先、角色停用、成员停用和成员到期表驱动测试
- [x] 11.3 增加管理授权区和业务授权区独立 revision、并发冲突、不互相覆盖和事务回滚测试
- [x] 11.4 增加角色级分配委派、可授权范围、复制越权、自我提权和人员管理员不能授予平台角色测试
- [x] 11.5 增加最后一名平台管理员、非最后管理员自撤权和服务账号管理能力限制测试
- [x] 11.6 增加按应用隔离能力和数据范围、“当前全部”不包含未来新增资源测试
- [x] 11.7 增加 compatibility/strict 模式、显式 deny 禁止旧回退和平台管理员无业务旁路测试
- [x] 11.8 增加业务能力与应用装配、Agent publication、平台启用、角色和只读风险交集测试
- [x] 11.9 增加 job 创建、排队撤权、执行中撤权、投递前撤权和安全通知状态测试
- [x] 11.10 增加权限解释与审计脱敏测试，扫描密码、Token、Secret、模型 API Key 和消息正文泄漏
- [x] 11.11 增加 typed API、CSRF、资源级后端授权、中文 field errors 和未知能力拒绝测试
- [x] 11.12 运行完整后端 pytest 与 Ruff，修复所有回归且不得弱化既有安全断言

## 12. 前端自动化与构建

- [x] 12.1 增加导航能力显示、路由拒绝和只读授权区测试
- [x] 12.2 增加角色列表/详情加载、能力依赖、资源级勾选和高风险确认测试
- [x] 12.3 增加分区 revision 冲突、原子错误、未保存离开提示和中文错误测试
- [x] 12.4 增加人员多角色、成员有效期和钉钉绑定多个初始角色/仅绑定/事务失败测试
- [x] 12.5 增加权限模拟、旧策略兼容标记、Agent 安全上限阻止和高级拒绝摘要测试
- [x] 12.6 运行前端 Vitest、TypeScript typecheck、ESLint 和 production build，修复全部回归

## 13. 容器与真实运行链路验证

- [x] 13.1 运行 OpenSpec strict validation，确保 proposal、9 份 delta specs、design 和 tasks 全部有效
- [x] 13.2 备份本地 PostgreSQL 并记录恢复验证结果，不修改或清理现有未提交工作和授权数据
- [x] 13.3 重建并启动 `api-server`、`admin-web`、`agent-worker`、`channel-dispatch-worker` 和相关依赖容器
- [x] 13.4 验证容器 health、migration、RabbitMQ consumer、前端静态资源和兼容模式状态
- [x] 13.5 使用真实浏览器完成登录、菜单按权限显示、角色创建、复合权限勾选、成员分配、有效期和权限模拟
- [x] 13.6 在 compatibility 模式证明现有旧授权链路不回归且 decision trace 明确标记旧兼容来源
- [x] 13.7 在受控测试角色上启用 strict 模式，证明未授权用户拒绝、授权用户允许、跨应用数据范围拒绝和写工具不可选择
- [x] 13.8 使用真实钉钉私聊消息验证 Runtime → Inbox → Outbox → RabbitMQ → Job → Worker → Tool → Delivery 完整授权链
- [x] 13.9 使用真实钉钉群聊消息验证按原发送人授权、群聊回复路由和权限拒绝中文提示
- [x] 13.10 验证绑定钉钉身份并原子分配初始角色后用户可立即使用应用，选择仅绑定时明确拒绝且候选立即消失
- [x] 13.11 验证任务排队、工具调用前或投递前撤权时分别 fail closed，业务结果不被越权投递
- [x] 13.12 查询数据库证明本 change 未删除旧用户、身份、角色、成员、permission policy、platform grant、运行记录或审计
- [x] 13.13 检查相关容器结构化日志和审计摘要，确认不含 Client Secret、Token、模型 API Key、密码或消息正文
- [x] 13.14 记录完整测试数量、容器镜像/状态和真实链路证据，只有全部通过后才允许开始独立重置 change
