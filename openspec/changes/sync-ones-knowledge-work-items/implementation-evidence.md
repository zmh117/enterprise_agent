# 实施证据（进行中）

日期：2026-09-21。仅本地代码与隔离测试证据；正式数据库、知识发布、角色授权与服务未变更。

## 多类型预检与分块

- 工单全量 CLI dry-run：4820 条、1 个项目、3345 条关联观察、4705 个图片引用；规范化后 1 条描述缺失。
- 需求全量 CLI dry-run：17388 条、33 个项目、8947 条关联观察、778 个图片引用；规范化后 12631 条描述缺失。真实子类型按用户确认保留。
- 两次均为 preflight_passed，未加 --commit、未加载数据库配置、不下载图片、不调用外部模型；输入摘要见 preflight-evidence.md。
- 新类型采用 ones-work-item-text/v1 和 ones-work-item-chunks/v1；旧 defect 的规范化、块和 Embedding profile 黄金指纹测试通过。

## 候选状态与迁移

- 新增未发布迁移 141（标题字段证据）与 142（同步绑定、运行、候选；pending 和已激活观察水位）。未修改既有 100–140 迁移。
- 候选保存不改 current_revision_id、当前类型或收录。关系绑候选修订。新文档 pending 不出现在当前分块/向量输入中。
- 来源排他锁与唯一有效运行约束；冻结基线按最多 200 行短事务保存，页游标与事实原子提交，可从中断恢复。
- 来源更新时间单独前进时复用旧正文版本，候选记录观察水位；激活前不推进当前水位。跨类型只调整候选成员。
- 候选 ChunkRepository/VectorRepository 从冻结成员读取，禁止借构建适配器查询未激活证据。后续已补候选索引（见第 4 组）；激活与定时 worker 尚未完成。
- 旧直写导入在同源已发布或存在有效候选运行时拒绝；完整旧输入的只读重放保留。

## 已执行验证

- 知识模块、迁移、事实源及测试分层扩展回归：824 passed、34 skipped。跳过项包含环境依赖测试，不代表已验收。
- 隔离 PostgreSQL 定向：21 passed，涵盖新建、140 升级保留正文/块/索引引用、迁移重放、来源排他、候选不可见和候选分块。测试后又补了冻结页恢复及仅时间戳变化测试，定向 18 passed、1 skipped（该跳过需单独 PostgreSQL migration fixture，前述 21 项已跑过该路径）。
- PostgreSQL 曾检测到新 141 迁移约束名选择错误，已修正未发布文件，并在新的隔离测试库从旧迁移重新验证。未在正式库执行错误或修正后的迁移。
- SQLite 与 PostgreSQL 中文注释覆盖、事实源清单、OpenSpec strict 和当前定向 Ruff/diff 检查通过。

## 候选向量代际（第 4 组）

- 新增未发布迁移 143：可信零成员索引、同步归属/来源/待清理时间与 RETIRED 状态；迁移不清理任何 collection。
- 候选索引使用 run + KB 的稳定身份；无变化语料拒绝创建新代际。指定旧索引、完整模型配置与模板/清洗 profile、文本摘要均匹配且旧点身份/向量有效时才复制数值，新 payload 使用候选修订引用。
- VectorService 补逐点向量有效性验证与零文档/零块处理；空版本不调用 Embedding。写入后崩溃重试保留同一候选 collection。
- 新向量代际 + 旧向量合同测试：38 passed、1 skipped；阶段状态、迁移与最小权限 PostgreSQL 定向：24 passed。向量端目前使用合成 Qdrant，尚非独立 Qdrant 服务验收。
- 代际清理用例仅处理同步 run 明确归属的 collection，保护所有发布/草稿引用、活跃来源和最近两个成功代际；首次无引用后等待至少 10 分钟，删除前在与发布操作共用的来源锁内复查。保留原始修订、块和数据库索引事实，已回收索引标记 RETIRED。未执行真实 Qdrant 清理。
- 容量检查按配置的数据卷路径读剩余空间，索引创建及每批写入前均核对保留空间；不足时保留失败检查点，不清理其他索引。worker/Compose 挂载及真实容量验收尚待第 7/8 组完成。

## 发布摘要与兼容接续（5.1–5.2）

- 新增未发布迁移 144；数据库新头为 144。旧修订 configuration_version=0 继续按原连接模式判定 2/3，迁移不重算摘要、不重新验证、不发布。
- 新资源修订使用版本 4，只摘要所选 KB 的成员、当前修订及分类，同时固定原内容连接与向量身份；同源其他 KB 的变化不影响未变的本 KB。内容读取权限补充上述元数据字段，不授予同步表或业务正文写权限。
- KnowledgeScopeUpgrade 为显式管理员 application 用例，要求完整维护资源集合和期望修订，独立逐点核验旧索引后在短事务中追加不可变修订与验证证据。保持 KB/连接/索引/角色授权不变；管理员草稿、停用、版本冲突及未知摘要均拒绝。CLI 装配仍待 7.1，不在服务启动或 migration 中自动执行。
- 资源建草稿、保存和发布与回收共用来源锁；停用仍可立即执行，兼容接续提交前 CAS 会拒绝并发停用，不能重新启用资源。

## 本轮最终验证

- 知识模块、迁移运行、事实源及测试分层扩展回归：855 passed、34 skipped；跳过项仍不作为已验收证据。
- 发布范围/冲突与代际保留专项：41 passed。
- 候选和迁移定向：21 passed，包含隔离 PostgreSQL 的来源排他、候选不可见、140 升级到 144 保留既有内容/块/索引引用，以及新建数据库和中文注释覆盖。
- 隔离 PostgreSQL 的 Knowledge MCP 最小权限定向：21 passed，包含新列可读、同步和业务敏感表不可任意访问、禁止业务写入及额外授权检测。
- Ruff、OpenSpec strict（全量 18 项）与 git diff 检查通过，事实源专项 7 passed。上述 PostgreSQL 使用任务专属临时容器和合成数据；测试结束后已移除该容器及 tmpfs 合成测试数据，没有删除用户数据卷。正式数据库仍未执行 141–144。

## 待确认：首轮在线同步的类型 ID 补全

离线文件缺少真实类型 UUID；在线接入补齐 ID/来源标记会改变当前内容摘要，即使 ONES 更新时间未变，仍会触发现行同版本冲突规则。已向用户提问，尚未改变规则或实现例外。

建议仅在更新时间、标题、正文、其他业务字段及分类完全不变时，允许填充缺失类型 UUID 并追加元数据修订，复用原向量；非空 ID 改写、正文/类型或其他同时间戳差异继续拒绝。待用户确认后更新 design/delta 与对应测试，再实施在线适配器。

## 尚未完成的边界

尚未正式入库工单/需求、运行其全量 Embedding、建立新 Qdrant collection、进行旧缺陷发布摘要兼容接续、激活或开放新 KB。每小时采集保持未启用；未连接真实 ONES。任务勾选以 tasks.md 为准。
