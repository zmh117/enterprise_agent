## 1. 实施基线与安全预检

- [x] 1.1 核对当前 canonical、相关 active change 的实际接口、迁移头和工作树；确认新任务不修改既有验收记录或历史迁移。
- [x] 1.2 为指定工单/Story 输入建立仅输出安全统计的预检报告：文件摘要、详情/list 集合、跨批身份冲突、类型/时间戳、标题与描述完整性；由受限程序重新核对 4820/17388 条输入，不向终端或模型展示业务正文。
- [x] 1.3 核对现有缺陷 source/KB/profile/index 和所有同源已发布资源的摘要版本；形成正式导入前兼容接续清单，不执行发布、授权或原始导出脚本。

## 2. 多类型导入与清洗分块

- [x] 2.1 在 domain 增加显式类型映射和来源版本比较规则，Story 使用 requirement；补未知类型、同版本冲突、类型迁移及旧文件重放测试。
- [x] 2.2 扩展详情/list 解析与导入 application/仓储的分类、KB 元数据和验证统计，保留稳定 document ID、真实跨类型关联及旧 defect 默认行为。
- [x] 2.3 增加工单/Story 标题-only 与质量标记处理，使用合成输入测试缺标题、纯图片、HTML 空壳、安全清洗和稀疏描述，不补造正文。
- [x] 2.4 增加版本化工单/Story 分块及 Embedding text 模板，保留字段来源、offset/hash 和文本上限；回归旧 defect profile/块身份不变。

## 3. 候选状态与持久化迁移

- [x] 3.1 新增前向迁移保存同步绑定、运行、候选引用与 pending 文档状态；同步 SQLite/PostgreSQL catalog、事实源、中文注释和测试分层登记，DDL 仅由 Migrator 执行。
- [x] 3.2 实现 source 排他运行、配置修订、阶段/检查点和候选项目的 application ports 与仓储；验证崩溃恢复、唯一约束及过期配置拒绝。
- [x] 3.3 实现冻结基线成员加候选覆盖的读取与候选修订/关系保存，保持现有 current 指针和成员不变；在旧 CLI 增加已发布内容直写门禁。
- [x] 3.4 适配候选 chunk_set 和向量输入读取，测试新文档 pending 不可见、旧版本保持可读、跨类型成员变更及只更新时间的无操作批次。

## 4. 向量代际与复用

- [x] 4.1 实现候选索引的稳定身份、冻结清单和断点续跑；无变化轮次不新建 collection、不编码、不发布。
- [x] 4.2 实现从明确旧索引读取向量并按完整 profile/模板/embedding_hash/维度验证复用；新 payload 必须绑定候选修订，补错库、错模型、无效向量测试。
- [x] 4.3 扩展完整索引验证及空库 READY 合同，覆盖逐点身份与摘要、不只是计数；空成员不编码且不能沿用旧成员点。
- [x] 4.4 实现仅限同步归属派生 collection 的保留与清理门禁、当前/上一成功版本保护、10 分钟等待及引用复查，容量不足安全暂停；合成测试不得删除真实历史索引。

## 5. 资源兼容接续与原子激活

- [x] 5.1 新增 KB 范围配置摘要版本及验证路径，保留旧版本判定；测试同源工单/Story 变化不影响未变缺陷 KB。
- [x] 5.2 提供显式旧发布兼容接续入口，固定原 KB/索引/连接/权限并独立验证；覆盖未知版本、未纳入维护资源、管理员草稿和停用冲突，不在迁移中自动发布。
- [ ] 5.3 实现同库短事务激活 current 修订、成员/关系、新不可变资源修订、发布指针及水位；跨类型相关 KB 一起提交，异库自动写入在启用前明确拒绝。
- [ ] 5.4 加入固定资源绑定及期望配置/内容修订 CAS，不覆盖管理员草稿、连接、模型或停用状态；新 KB 内容就绪不自动首次发布或授予角色。
- [ ] 5.5 补激活前失败、事务冲突、提交后崩溃重试、空库激活与正在检索时切版测试；保持 MCP 双权限和最长 120 秒预算。

## 6. 受管 ONES 采集适配器

- [x] 6.1 将指定导出器必要分页、分片、详情与字段映射逻辑迁入 knowledge infrastructure，剔除字面量认证材料、任意脚本执行、图片下载和关闭 TLS 校验路径。
- [x] 6.2 使用现有受管身份/HTTP 边界实现固定 Provider、Team、项目/类型 UUID 范围及安全凭据引用；采集写身份与 MCP 只读身份分离，默认不要求真实配置。
- [x] 6.3 实现完整枚举与已有 UUID 详情版本核对，持久化页/分片检查点；补重复页、无进展、截断、最小分片仍超限和详情缺项测试。
- [x] 6.4 实现旧项更新、无变化、类型迁移和失败恢复编排；补 401/403/404/5xx/超时及范围可见性下降测试，不据缺失推断删除或成功空库。
- [x] 6.5 将更新时间优化保留为未启用能力边界，文档说明真实过滤/排序合同与周期全量对账的启用前提；首版不得伪称增量过滤已获真实验收。

## 7. 默认停用的每小时同步入口

- [ ] 7.1 增加 worker/CLI 装配及 binding 管理入口，串行运行采集、候选、分块、索引和激活；只提供本任务需要的命令，不新增模型写工具或通用调度层。
- [ ] 7.2 实现 3600 秒触发、重复 tick 合并、重启先恢复、禁用/取消有界停止和安全运行摘要；补长运行、并发启动与时钟边界测试。
- [ ] 7.3 在 knowledge 下提供可选同步 Compose 和无真实凭据的配置示例，默认停用、显式启动与 binding enabled 双门禁，不改其他环境默认部署。
- [ ] 7.4 编写配置、单次执行、状态/恢复、容量保护、禁用与回退手册，区分离线导入、定时同步、首次发布授权和内容自动换版，不创建助手定时任务。

## 8. 本地代码与隔离链路验收

- [ ] 8.1 执行类型化导入、关系、稀疏文本、分块、索引、资源及 MCP 定向回归；确认所有新增测试已登记测试层级。
- [ ] 8.2 在隔离 PostgreSQL 验证迁移、source 排他、候选不可见和多 KB 原子激活，SQLite 行为保持等价；不把 SQLite 通过当正式数据库验证。
- [ ] 8.3 用隔离 Qdrant 和合成 Provider 验证首次构建、向量复用、失败保留旧版、无变化零编码、断点续跑和空库；故障注入仅作用于测试归属资源。
- [ ] 8.4 验证目录/检索的同应用角色 KB 范围与本人 ONES 权限，覆盖采集账号广权限不传递、不同应用隔离、停用和并发换版；明确使用合成 Provider。
- [ ] 8.5 完成可选 Compose 配置校验、Markdown 链接、严格 OpenSpec、静态检查和 git diff 检查；报告不相关回归失败，不改旧验收状态。

## 9. 正式本地工单与 Story 流水线

- [ ] 9.1 执行前重新核对输入摘要、目标 schema head、非终态 Job、容量/索引保留空间及缺陷发布基线；必要服务重启和兼容接续按明确维护授权执行，采集仍停用。
- [ ] 9.2 在批准范围内运行 Migrator 和旧知识发布的 KB 范围兼容接续，验证缺陷旧数据与索引可用；发生冲突停止后续正式导入。
- [ ] 9.3 正式候选导入预检通过的全部工单和 Story，记录安全入库/版本/关联统计及缺描述数量；重放验证不新增重复项，不能用部分成功冒充全部完成。
- [ ] 9.4 完成两类正式清洗分块，核对逐文档覆盖、稀疏块标记、块边界和摘要；保留既有缺陷 profile/向量不变的证据。
- [ ] 9.5 在本地/内网 BGE-M3 做有界吞吐基准并报告估算，执行可恢复全量向量构建与逐点完整校验；后台进度仅输出安全统计，不使用外部模型。
- [ ] 9.6 激活两类完整内容与 READY 索引，报告实际文档/块/点数、版本、失败项和关联解析；确认默认同步停用、角色/应用授权未改。
- [ ] 9.7 在明确批准的新资源发布和现有授权范围内验收 Knowledge MCP 集成；本机使用合成 ONES 时注明证据边界，未批准发布或真实权限验收保持未完成。

## 10. 未来真实 ONES 环境验收（保持待办）

- [ ] 10.1 在能够访问真实 ONES 的目标环境配置受管只读身份与明确来源/范围，验证 TLS、分页、来源版本单位、新增和已有项修改；不从本机脚本提取认证值。
- [ ] 10.2 显式单次采集验证三类更新、重放、失败不换版、自动切版和实际用户权限，并人工评估标题-only Story 与正文记录的检索效果；真实证据未取得不得勾选。
- [ ] 10.3 获得该环境启用授权后开启每小时调度，观察至少一个实际周期、长运行恢复及关闭行为；当前本机按用户要求保持停用。

## 11. 新版测试数据替换与混合召回（2026-09-23 确认）

本节取代本次执行任务 9 的旧输入范围；不补勾旧全量数据验收。在线采集/每小时 worker 不属于本次前置工作。

- [x] 11.1 实现 keep_ids_v1 严格预检、ID/名称/字段字典规范化、Story 与子任务合并及父子证据，验证真实输入 2000/2000/8654 与仅统计输出。
- [x] 11.2 实现明确三 KB 候选替换，不由样本缺项推断业务删除；新版本化分块保留名字上下文与标题-only，旧发布构建期间保持可用。
- [x] 11.3 增加同代 Qdrant BM25 sparse＋BGE-M3 dense、中文分词配置、等权 RRF；补两路完整性、错版本/错库/失败和在线权限预算回归。
- [x] 11.4 补本地纯 BM25/dense/hybrid 对照评测及安全汇总，区分合成、自查询与人工标签，测试权重与候选上限边界。
- [ ] 11.5 经批准完成本机迁移/配套服务更新，正式候选入库与分块、本地基准及可恢复双路索引构建。
- [ ] 11.6 完整核验并切换三个 KB 当前内容/已发布资源索引，不改变权限或覆盖草稿；重复执行幂等，旧数据不参与新检索。
- [ ] 11.7 报告真实计数、对照评测结果与证据边界；人工问题/真实 ONES 未取得时明确待验收。
