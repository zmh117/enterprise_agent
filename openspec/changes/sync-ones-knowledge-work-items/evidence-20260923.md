# 2026-09-23 本机测试替换阶段证据

这是执行记录，不是 canonical 规范，也不是完整真实 ONES 验收。

## 授权与边界

用户明确要求以 `最近2000_保留ID` 替换三知识库测试内容并测试混合召回，随后批准 schema 140→144 和配套本机服务维护。不修改凭据、不删除业务卷、不启用定时采集；不下载图片/附件、不做 OCR，本轮不索引评论。

## 已完成

- 正式 migration 成功：head=144，applied=141,142,143,144。配套后端服务更新；Knowledge MCP 精确补列权限并通过 reader 合同检查。
- source 仍为既有 `ones-offline-export`，缺陷 KB ID 不变；新增工单/需求逻辑 KB，共享表而非每库独立建表。
- 候选运行 `e2a7b945-31a0-511a-bfaa-a7f34a607b69`，绑定 `ones-keep-ids-test-snapshot`，enabled=0，interval_seconds=3600。
- 严格预检/正式候选入库：缺陷 2000、工单 2000、需求 8654（Story 2000＋子任务 6654），共 12654。
- 与旧 5000 缺陷的对应：1876 个已有文档创建候选修订，10778 个新文档为 pending；3124 个旧文档不在本批输入，将在激活时移出新成员，不删除历史。
- 分块：缺陷 4055、工单 2006、需求 8656，共 14717；需求 6898 条无描述，使用真实标题块并标记缺失。
- chunk profile hash `c21ecb93740ceb0eeeb95310915ee275450182aa7b6075315e13fbec25c9b17d`，BGE-M3 profile hash `595254deaeae70c19815d4b42847cc468790a2742bbdf4b5bd6703cfefcf22f3`。
- 真实本机 Qdrant v1.19.1：合成中文文本经生产客户端完成 BM25、dense、RRF 查询及双命名向量检查；仅删除自己创建的临时 collection，旧索引未改。
- 真实平台管理员权限和已发布内容连接物理归属只读核验通过（同一 PostgreSQL），连接与凭据引用未改。
- SQLite/隔离 PostgreSQL 覆盖激活原子回滚、重放幂等、保留权限/旧发布、草稿/成员/授权冲突；隔离 PostgreSQL 实测通过，临时容器已清理。
- 知识模块及维护 CLI 的 mypy 66 文件通过，Ruff 通过。完整知识回归 835 通过、35 跳过（264.07 秒），跳过包括需显式外部/集成配置的测试；另已显式运行隔离 PostgreSQL 59 通过、1 跳过，以及替换事务 PostgreSQL 测试通过。
- 随后补充评测输出脱敏/服务失败不可冒充完整结果 2 测试通过；OpenSpec 全部 18 项 strict 校验通过，git diff --check 通过。

## 本地速度基准（每库按长度分层 100 块）

| 库 | 采样耗时 | 块/秒 | 全量估算 |
| --- | ---: | ---: | ---: |
| 缺陷 | 113.529 秒 | 0.881 | 4604 秒 |
| 工单 | 172.577 秒 | 0.579 | 3462 秒 |
| 需求 | 82.561 秒 | 1.211 | 7146 秒 |

合计约 4.23 小时，仅为估算，运行负载、实际长度分布及核验开销会影响结果。

## 索引构建期间记录（已结束）

后台容器 `ea-ones-index-20260923` 使用显式 run-id 构建三库双路索引，Qdrant 数据卷只读挂载 `/qdrant_capacity` 做容量检查；开始时 Docker 盘约 60 GiB 可用。运行只输出安全统计，失败后可断点重放。

2026-09-23 09:14 UTC 启动，首批 128 个真实缺陷块已成功编码/写入。不是仅计划或空容器。API 与 Knowledge MCP 配套更新后再次通过健康检查。

以上为构建期间的状态；以下是完成后的实测结果。当前只有缺陷资源已发布；工单/需求不自动首次发布或授予角色权限。

## 完成后的本机核验

- 后台索引容器 `ea-ones-index-20260923` 于 2026-09-23 12:32:23 UTC 正常退出（exit 0）。同一 run-id 的三套索引均为 `READY`，逐点身份、引用与双路向量由受管激活入口核验通过。索引项状态 `INDEXED` 共 14717，等于三个候选的预期块数之和。
- `activate` 使用既有真实平台管理员身份在 API 维护环境执行，退出码 0；运行阶段为 `ACTIVATED`，错误码为空，水位已提交。再次以相同 run-id 执行也成功，缺陷资源发布修订未增加，验证提交后重放幂等。
- 当前收录与新索引计数如下；旧缺陷中 3124 条仅退出当前成员集合，不删除历史文档、修订或旧索引。

| 知识库 | 当前文档 | 新分块 / 索引点 | 索引状态 |
| --- | ---: | ---: | --- |
| 缺陷 `ones-defects-offline` | 2000 | 4055 / 4055 | READY |
| 工单 `ones-tickets-offline` | 2000 | 2006 / 2006 | READY |
| 需求 `ones-stories-offline` | 8654 | 8656 / 8656 | READY |

- 既有缺陷资源 `ones_bug` 仍启用、无草稿，资源修订为 10，当前不可变发布修订为 3（`configuration_version=4`），指向本次新索引；旧发布修订及索引仍在。API 维护环境对该资源的 `resolve` / `recheck` 成功。平台知识资源总数仍为 1，角色知识库授权行仍为 1；本次未创建工单/需求资源、未授予新权限，也未修改应用 Publication。
- 同步绑定仍为 `enabled=0`、`interval_seconds=3600`；本机无真实 ONES 连接，也未启动每小时采集。API 与 Knowledge MCP 均通过健康检查，schema head 保持 144。

## 同代三路自查询对照

每类知识库抽取前 10 个标题已知项和前 10 个合格正文片段已知项，三种模式各 60 次，共 180 次查询，服务失败 0。以下为文档级自身命中，不是人工相关性召回率；延迟是 6 个 cohort 各自 p50/p95 的范围，不是所有请求的合并分位数。

| 方法 | 自身命中 @1 | @5 | @10 | MRR@10 | cohort p50 范围 | cohort p95 范围 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BM25 | 52/60 | 59/60 | 59/60 | 0.921 | 55–59 ms | 60–77 ms |
| BGE-M3 dense | 44/60 | 57/60 | 60/60 | 0.826 | 269–486 ms | 338–549 ms |
| 等权 RRF hybrid | 51/60 | 59/60 | 60/60 | 0.904 | 290–496 ms | 354–523 ms |

需求正文 cohort 中，BM25 自身 @1 为 5/10，hybrid 为 3/10；hybrid 自身 @10 为 10/10，BM25 为 9/10。等权融合在这组自查询上并未全面优于 BM25，不能据此宣称业务 Recall 提升或 1:1 权重最优。真实用户问题、人工相关性标签和真实 ONES 本人可读权限尚未验收；新工单/需求资源的首次发布与授权仍待管理员显式操作。

仍无真实用户标注问题与真实 ONES 环境，不能声称业务 Recall 提升、权重最优或完成 KB＋ONES 实际用户验收。
