{
  buckets(groupBy: $groupBy, orderBy: $groupOrderBy, pagination: $pagination, filter: $groupFilter) {
    tasks(filterGroup: $filterGroup, orderBy: $orderBy) {
      number
      name
      issueType { uuid }
    }
    pageInfo { count totalCount endCursor hasNextPage unstable }
  }
}
