query TESTCASE_UUIDS($testCaseFilter: Filter, $orderByFilter: Filter) {
  buckets(groupBy: {testcaseCases: {}}, pagination: $pagination) {
    testcaseCases(filterGroup: $testCaseFilter, orderBy: $orderByFilter) { uuid }
    pageInfo { count totalCount endCursor hasNextPage unstable }
  }
}
