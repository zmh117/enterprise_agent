Use this skill when database state can explain a symptom.

Rules:
1. Only use read-only SELECT or WITH queries.
2. Bound result size and avoid sensitive columns.
3. Explain status fields, enums, and relationships using retrieved context.
4. Never suggest direct production updates.

